# 缓存写入统一定价修复（2026-09-17）

## 问题与范围

GPT-5.6 Sol 的现有表达式包含 `cc` 缓存写入费用。旧工作台能够试算和保存成本，但统一价格发布器拒绝包含此变量的公式，导致选择模型后出现“当前向导尚不能完整报价”，无法完成发布。

本次补齐缓存写入报价、预览、确认、持久任务、配置写入与回读核验。客户继续使用统一单价，不按输入长度分档。代码发布不自动修改客户价格，价格仍由管理员核对预览后确认发布。

## 实现

- 新统一定价任务采用 V5，独立处理输入、输出、缓存读取、缓存写入和 1 小时缓存写入；只写既有表达式字典，保留模型其他配置和公共/客户专属分组倍率。
- 当前公式使用的缓存类别在表单标为必填，缺少报价会给出具体字段提示。显式零与缺失严格区分；缓存变量即使系数为零，也影响 new-api 对普通输入 token 的归一，不能删除零项。
- `cc` 是通用缓存写入类别，在 Claude 语义下对应普通/5 分钟写入；`cc1h` 单独处理。工作台使用通用“缓存写入”标签，1 小时字段仅在实际需要或有报价时出现。
- 参考价查询保留两类缓存写入报价，不将缺失当作零，不用 1 小时报价替代普通写入报价。售价仍按 `基础价 × 售价倍率 ÷ 每元到账额度` 计算。
- `cache_write_1h` 为可选 JSON 字段且没有默认值，旧成本数据、旧签名指纹不插入新字段。V3/V4 历史任务继续按各自旧生成器重建，不能被静默转为 V5。
- 发布前重新核对成本版本、模型映射、影响分组和计费配置；发布后核对持久配置和运行时公式，完整核对缓存价后才激活 Portal 目录。失败与不确定写入继续使用原有恢复机制，不盲目重放。
- V5 预览中的旧公式若仅部分分档使用某缓存变量，将缺项显示元数据按实际引擎语义记为有效零；原公式和历史任务保持原文。页面继续隐藏旧长度档价格对比。

## 验证

- 完整测试：331 个文件通过、1 个既有跳过；5039 项通过、2 项既有跳过。
- TypeScript、生产构建通过；ESLint 0 error、93 个既有 warning。
- 后端定向 16 文件 / 450 项：覆盖保存到确认/worker/回读/目录完整链路，以及缓存配置延迟、写入前后断线、持久化不一致、事务失败、任务篡改、旧任务恢复、缺失与显式零。数据库与网络边界使用测试替身。
- 独立运行官方 new-api rc23 引擎，锁定 revision `0ab02020603d22e5613bc4cf46bfab06f8567769`，160 个无收费向量通过：包括缓存 token 重叠、GPT/Claude 语义、缺失/零、长短输入和公共/专属倍率。未调用模型供应商。
- 另有 8 个官方引擎向量验证跨档独有缓存变量的全局归一，合计 168/168 通过。
- 隔离的生产构建通过真实 HTTP、PostgreSQL 和 MySQL 完成 14 组检查、17 次 HTTP 请求；new-api HTTP 为合成边界。4 个 V5 任务全部成功、3 次表达式写入、5 版目录，覆盖缺失报价、显式零、独立缓存类别、专属倍率、运行态滞后重试、重复确认及同价不重复写入。未调用真实供应商或写生产价格。
- 前端覆盖必填字段、按需显示 1 小时字段、旧成本 JSON 往返、缺失报价、完整缓存预览以及拒绝不完整确认。
- 网络恢复后，2026-09-18 使用浏览器在上述隔离环境选择 GPT-5.6 Sol，成功加载已存规则并进入发布预览；缓存写入和 1 小时价格独立显示，旧阻断提示及长度分档对比均不存在。浏览器检查只预览，未重复提交发布任务。

## 发布约束

本次没有数据库 migration、依赖、环境变量、Nginx 或 Cloudflare 变更，不修改 new-api 源码。按 `dev → prod → VPS` 发布，仅重建 Portal。

上线前使用新鲜备份和只读计费快照，保留管理员已发布的 GPT-5.5 V4 历史，不能采用旧“任务数量为零”的基线。候选容器通过独立 GET 白名单代理访问真实 new-api，保存/预览仅在隔离 PostgreSQL 副本中进行，不对真实 new-api 确认发布。

回滚旧镜像前必须先停写并检查没有 V5 任务或新增 `cache_write_1h` 成本数据；有新格式数据时应修复前进，不能让旧代码误读新任务，也不能恢复数据库抹掉用户的业务操作。

## 生产发布结果

- 应用源码 `6138d5b510fc51fb06a84f71fd408fb12e3e17c9`，按 `dev → prod → VPS` 发布；运行镜像为 `sha256:2f3426bfc78e6657588e6e6a71ddee16127c2c24b5103ef8a476c2f62c0c6f41`。后续纯文档归档不重建镜像。
- 北京时间 2026-09-17 21:32:36–21:32:52 切换 Portal。首次失败至恢复的探测跨度为 8.925 秒，随后稳定；Portal restart count 0，启动错误 0。PostgreSQL、new-api 和 MySQL 容器及启动时间保持不变。
- 备份与证据目录：`/opt/backups/silkroadai-portal/releases/cache-write-20260917-084958/`。`portal.sql.gz` 为 0600，gzip 完整性和实际隔离恢复均通过；环境备份为 `/opt/silkroadai-portal/.env.bak.cache-write-20260917-084958`。
- 回滚镜像标签为 `silkroadai-portal-portal:rollback-cache-write-20260917-084958`（旧应用 `d53fd79`）。回滚前必须遵守上方 V5/新成本格式兼容约束，不能用恢复旧数据库覆盖业务操作。
- 新鲜生产备份克隆完成 14 项主检查：真实模型与公式只读、缓存缺项拒绝、完整缓存预览、显式零、历史版本及过期版本拒绝通过；临时容器、数据库和环境文件已清理。
- 线上只读成本能力确认 GPT-5.6 Sol 企业档可发布统一价格，必需缓存类别为 `cache_read`、`cache_write`；固定图片 SKU 能力保持。5 个公网脚本与新镜像内容一致。
- 50/50 源站及公网检查通过；80 条 migration checksum、11 张业务表、15 项 new-api 价格配置与客户权限摘要保持。已有 GPT-5.5 发布记录和售价未改动。
- 本次仅部署功能，没有替管理员发布生产价格，没有收费推理、支付或真实客户 Key 撤销。保存草稿、预览、确认、worker 与回读闭环已在隔离环境验证，实际售价仍由管理员确认发布。

使用方式：刷新定价页，选择 GPT-5.6 Sol 和档次，填写基础报价、充值比例和双倍率；补齐标为必填的缓存报价，点击「保存并预览此价格」，核对后确认发布，任务显示「已生效」才表示两端核验完成。测试用缓存价格不能当作真实上游报价照填。
