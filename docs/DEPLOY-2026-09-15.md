# 2026-09-15 上游 #465 生产发布

## 发布结果

operator 明确「上线吧」后，已部署测试通过的 `prod@047e64cf385b5b46686a77378aafa87d32998db1`。北京时间 **09:22:20–09:22:35** 切换 Portal，源站探测共 26 次、13 次失败，首次失败至首次恢复 HTTP 200 为 **8.124 秒**。这是采样中断窗口，不是逐个客户请求的实测影响。

- 运行镜像：`sha256:0efc20216cc25f8585668d752f842621c27e5cb40f9cd62892e277f50f712ca9`，标签 `silkroadai-portal-portal:release-047e64c`。
- Portal 容器：`51bf51355805b045c5e09594ec331e4436fcada9eb31670e31539403fd9eaee1`，启动于 `2026-09-15T01:22:21.279933514Z`，restart count 0。
- `main` 保持上游 `33ebe6b`；dev 已完成集成与测试，再 fast-forward 至 prod。发布后的纯文档提交不重建应用镜像。

本批唯一运行时变化是 ominiapi25 接收全部五档 quality 及 auto/空值；响应合成、计量和 metadata 处理均为原有实现。逐提交、行为边界和回归负对照见 [同步报告](UPSTREAM-SYNC-2026-09-14-02.md)。**Image 2.5 与 Batch 继续显式关闭**，本次没有开通新图片线路。

## 备份与执行

发布目录 `/opt/backups/silkroadai-portal/releases/upstream-20260915-011013/` 权限 0700，备份和证据文件 0600。

| 项目       | 实际结果                                                                                          |
| ---------- | ------------------------------------------------------------------------------------------------- |
| PostgreSQL | 发布目录 `portal.sql.gz`，657638 bytes；dump、gzip 完整性及真实隔离恢复通过                       |
| 环境备份   | `/opt/silkroadai-portal/.env.bak.upstream-20260915-011013`；实际生产 `.env` 发布前后 SHA-256 一致 |
| 其他备份   | Nginx 配置、new-api 九项价格/计费选项中实际存在的值、目录/Key 摘要、77 条迁移清单、旧实例身份     |
| 旧 Git     | `2c84623f31d688e22e1e95a99cfeb3139d23b392`，旧运行应用为 9 月 14 日 `4413e61`                     |
| 旧镜像     | `sha256:6c8d7e47ec47f11a7e525fd8fc4aab729c7b01dee6f337c8434b1ba671237b58`                         |
| 回滚标签   | `silkroadai-portal-portal:rollback-upstream-20260915-011013`；本次没有触发回滚                    |

09:11:44–09:15:34 从精确 Git 归档在 VPS 构建，使用生产 Compose 的公开 build args；构建上下文排除 `.env*` 与运行时目录。旧 Portal 在构建和隔离检查期间持续运行。本批无新增 migration、环境变量、依赖、Nginx 或 Cloudflare 配置变更。

候选容器使用本次备份恢复的随机临时数据库、随机管理员/JWT 凭据，关闭调度器且不发布主机端口。14 项检查全部通过：备份目录一致、77 条 migration/checksum 正常、非 root uid 1001、固定 RSA 公钥可读、真实价格页无数据库降级、真实 rc.23/MySQL 价格预览正常、目录和发布任务保持不变。临时容器、数据库及含密环境文件均已清理，并验证消失。正式 Portal 的调度器未被候选环境的关闭设置覆盖。

切换前两次确认无未结算视频、活动 Batch、非终态价格任务、活动协调任务或 in_flight 写入，并连续三次采样无现有 3002 连接；这只是空闲观察，不是原子停写。生产 Compose 使用 `up -d --no-deps --no-build portal`，只替换 Portal。PostgreSQL、new-api、MySQL 的容器 ID、镜像和启动时间均未变。

本次运维 SSH 全程指定 `ControlPath=none`、`ControlMaster=no`、`ClearAllForwardings=yes`，不复用用户隧道 master。开始时本机没有 3000 监听或该 control socket；本次未创建、删除或重建用户隧道。

## 上线验收

| 检查         | 实际结果                                                                                                               |
| ------------ | ---------------------------------------------------------------------------------------------------------------------- |
| 服务         | 四个容器 running，PostgreSQL healthy，Portal restart count 0；Nginx 配置有效且运行正常                                 |
| 数据库       | 77 applied，0 unfinished，0 rolled back，全部 checksum 匹配，无增量 migration                                          |
| 数据保持     | 29 模型、35 价格版本、6 档次、21 Key 的完整摘要与发布前一致；发布 jobs/writes/active/in_flight 为 0                    |
| 计费与拓扑   | 全局及 13/13 用户继续 new-api 计费；9 项拓扑错误计数均为 0                                                             |
| 持久鉴权     | 13/13 活动用户 opaque 非 JWT token 的真实 GET `/api/user/self` 身份匹配，凭据没有轮换                                  |
| 公网模型列表 | 管理员自己已有 Key 的普通/Python-urllib UA 均 HTTP 200，同为 4 模型，3 项现行价格核对一致                              |
| 固定图片 SKU | Portal 与 new-api 配置换算均为 1K ¥1、2K ¥1.5、4K ¥2                                                                   |
| 价格双读     | ModelRatio、CompletionRatio、ModelPrice、GroupRatio 的 API 前后读数与 MySQL 一致，部署前后及额外计费选项均未变         |
| 只读权限     | 既有账号仍仅 global USAGE 与 SELECT `new_api.options`，固定 RSA 公钥 hash 不变；不把 Docker 内网 SQL 误称 TLS          |
| 页面与预览   | 真实生产价格页 HTTP 200、包含在售模型、无数据库降级；Kimi 单次 preview 成功，影响 1 档、3 条提示，未创建任务或写入价格 |
| 功能开关     | Batch、Image 2.5 均显式 false，内部入口实际返回各自预期 503 错误码                                                     |
| 网络         | 50/50 源站及公网检查首次全部通过                                                                                       |
| 日志         | 成功读取切换后的 Portal 日志，error/fatal/failed 行数为 0                                                              |

50 项覆盖 apex/www 登录、模型和价格页；API 假 Key 401、管理路径 404；四类内部适配器在主站/www/API 的源站及公网 404；OpenAI/Gemini CORS 204；Google/GitHub OAuth start 302；主站与 API access log 不记录测试 query string。Gemini 仅验证无 Cookie 的 `x-goog-api-key` 模式。

独立的只读快照比较再次确认六张目录/Key/发布表、迁移、计费、拓扑、功能开关、只读权限和价格选项一致。最后生产校验在预览结束后执行，确认没有遗留发布任务、写入意图或目录变化。

## 验证边界与证据

开发门禁：完整测试 **309 files / 4178 passed / 1 既有 skipped**，含真实只读 new-api smoke；typecheck、两套 Prisma validate、生产构建通过，lint 0 error / 93 既有 warnings。本次生产配置构建、备份恢复及真实数据库价格页检查补齐了本地 PostgreSQL 不可达时的页面验证边界。

本次没有真实价格发布、收费推理/图片生成、支付或完整浏览器 OAuth 登录。价格预览和模型列表成功不代表这些操作已验收；上游 quality 回显、计量和合成 metadata fixture 也不能证明真实画质、厂商来源或签名。本次回滚预案未触发，不能称已实测回滚。

服务器证据位于上述发布目录，本机过程文件位于 `/tmp/llmroute-deploy-20260915/`。用户既有 `.env`、`.idea/vcs.xml` 与未跟踪需求文档保持原样，未纳入提交。
