# 按档次整组定价 · 2026-09-18

## 操作与范围

定价页主入口改为「按档次统一定价」：选择 Portal 档次，读取其准确对应的 new-api group 当前启用渠道模型；一次填写基础报价单位、充值比例、上游倍率和售价倍率，核对整组报价后提交一个发布任务。原单模型入口折叠保留，模型清单右上角的档次筛选继续保留。

已存成本优先；未保存的文本模型按精确模型名查询 LiteLLM 基础参考价，用户须确认上游采用这组基础数字，缺价可逐项补填。缺失缓存价格不当作免费。已不在 new-api 分组的旧 Portal 映射仅提示，不混入本次清单。

示例：上游充值 ¥1 到账 10 额度、上游 1.3 倍、售价 1.6 倍，最终组倍率为 0.16。基础输入价 5 对应成本 ¥0.65、客户售价 ¥0.80／百万 token。客户专属倍率替代公共倍率，其报价单独展示。

## 发布语义

- 新 V6 任务统一修改所选 new-api `GroupRatio` 条目。必须覆盖该组全部当前模型，最多 30 个，不允许漏选后悄悄影响漏选模型。
- 普通 token、支持的文本表达式、按次图片可以同组预览；不支持的计费方式、视频、未登记、失效归属及缺价均明确显示并阻止整组发布。
- 本项目采用统一文本单价。仅所选分组独有的模型可按已确认报价归一其基础公式；与其他活动分组共享模型时，基础计费必须等价，否则阻止，以免连带改动其他组。
- 保存预览会保存整组成本草稿，不会立即改价。确认后由持久发布任务执行，逐项记录写入；独立读取 MySQL 持久配置和 new-api 运行价格，全部一致后在一个 PostgreSQL 事务内激活整组目录价格。
- 旧 V1–V5 任务保持原语义。新 V6 任务存在时不得回滚不识别该格式的旧 worker；失败应在本版本续跑或向前修复，不能删历史强行回滚。

没有数据库迁移、环境变量、依赖、Nginx 或 new-api 源码改动。上线功能不等于替 operator 发布实际价格。

## 验证

- 完整测试 338 files passed / 1 skipped，5161 tests passed / 2 skipped；类型与生产构建通过；lint 0 error、93 条既有 warning。全仓格式检查仅原有 4 份文档不符合格式，本次文件格式通过，未改用户未跟踪需求文档。
- 真实 React 组件浏览器验证：选档次自动读取全组；10/1.3/1.6 报价；整组预览；修改参数使旧签名失效；已存版本重用；一次确认产生一个任务；缺价补齐；未登记／空组阻止；390px 窄屏；无控制台错误。
- 本地生产构建、真实 PostgreSQL／MySQL、隔离 HTTP new-api fixture：4 模型／2 分组，覆盖表达式、共享普通倍率、缓存及图片。部分模型请求与篡改签名在写入前拒绝；重复确认得到同一 V6 任务；持久配置已写、运行缓存滞后时不激活任何目录；恢复后不重复 PUT，整组完成；同价重复发布零写入；后续改倍率只更新 GroupRatio 一项，其他组保持不变；旧目录参考成本保留。
- 隔离验收未调用供应商，无收费推理或生产业务写入。

## 生产发布记录

- 代码 `01746ba21595f472970261aeb2dc66c4f7b158cb`，完成 `dev → prod` fast-forward 并推送；北京时间 16:35 切换 Portal。
- 运行镜像 `sha256:9299bab59e4828eae0e89ce7358c2ce26c3f44876e034be5d4f38fac935fbdfd`。干净 Git 归档构建，镜像无 `.env*`；旧 Portal 构建期间继续服务。切换探测 25 次、12 次短暂失败，随后恢复稳定，restart count 0。
- 备份与完整验证证据：`/opt/backups/silkroadai-portal/releases/group-pricing-20260918-082608/`；`.env` 备份 `/opt/silkroadai-portal/.env.bak.group-pricing-20260918-082608`，均受限权限；数据库 gzip 已验证并恢复到临时数据库验收，临时容器／数据库／凭据已清理。
- 生产数据隔离副本的只读候选验收、6 档次模型 GET、模型清单页面、定价新入口及精确 JS 内容校验通过。公网／源站 14 项检查通过，启动无 error/fatal。80 条 migration checksum 不变。
- 发布前后 11 张业务表摘要、客户档次权限、15 个 new-api 价格配置、环境变量均一致。PostgreSQL、new-api、MySQL 容器未重建；未创建生产定价任务、未保存实际成本、未改客户收费、未调用收费模型。

当前数据需由 operator 处理的事项已在页面列明：企业组 4 模型中 `gpt-5.4` 在 Portal 停用（其余 3 项报价齐全）；CCMax 两组分别 11／9 项报价齐全；Kimi 缺基础报价；图片组 6 项待补价、两个 Nano Banana 的目录类型与计费方式不匹配；Claude-K 组 `claude-sonnet-4-5` 停用。未擅自启用停用模型或修改已有计费方式；有阻塞项时不会部分修改整组倍率。
