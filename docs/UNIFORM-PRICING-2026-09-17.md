# 按倍率发布统一售价（2026-09-17）

## 需求纠正

operator 明确：了解上游动态计费不等于要求 Portal 继承阶梯。客户售价只按填写的基础报价、充值比例与售价倍率计算，不能随上下文长度切换。此前 `bf312e6` 保留阶梯的发布策略不符合这个需求，本次改正。

- 参考成本 = 基础报价 × 上游倍率 ÷ 每元到账额度。
- 客户单价 = 基础报价 × 售价倍率 ÷ 每元到账额度。
- 上游 1:10、倍率 1.3，售价倍率 1.6，基础输入 5、输出 30、缓存读取 0.5：参考成本为 ¥0.65 / ¥3.9 / ¥0.065，客户所有输入长度统一为 ¥0.8 / ¥4.8 / ¥0.08（每百万 token）。
- 成本是所填报价的估算，不声称覆盖供应商实际账单的全部策略。实际业务价格仍需管理员预览确认发布，部署代码不自动改价。

## 实现与兼容

新增 V4 `uniform_token` 发布意图，按输入、输出、缓存读取各自的绝对目标售价生成不含 `len` 条件的固定表达式。借用 new-api 原生表达式引擎，无须修改 new-api 源码；只写一个表达式选项，不切换模式或改变公共/客户专属分组倍率。相同上游模型名的所有关联组影响均在预览中列出，专属倍率仍替代公共倍率。

V1/V2 普通价格任务保持；旧 V3 已存任务按原意恢复，不能被新逻辑解释为统一定价。新旧预览签名不兼容，旧页面需刷新预览。保留来源、成本版本、持久写意图、未知写入禁止重放、管理 API/只读数据库/匿名运行配置三方核验。只有核验成功才写目录并完成任务。

目录继续使用兼容的 `billing_details` 结构，统一价格是一条没有长度上下限的记录；公开价格、后台当前价及历史准确显示“统一单价”。旧阶梯历史继续如实展示。新工作台显示统一目标价格，现有旧价格折叠为对比信息，不要求用户选择阶梯。

本次无 migration、环境变量、依赖、Nginx 或 Cloudflare 修改。针对现有文本表达式模型；未扩展复杂媒体公式、缓存写入或其他未安排功能。

## 开发验证

- 全部测试：331 files，330 passed / 1 skipped；4,983 passed / 2 skipped。包括真实只读模型列表 smoke。
- typecheck、生产构建通过，lint 0 error / 93 个既有 warning。
- 新 V4 计划/发布恢复测试及原 V3 回归均通过，覆盖独立输出/缓存价格、旧签名/版本/策略篡改、来源变更、SQL失败、运行缓存延迟、未知写入恢复和同目标不重复加价。
- 官方 new-api rc.23 同 revision 引擎 96 个向量通过：272000 边界两侧、长输入、缓存主体、纯缓存、公共 .16 / 专属 .18 均保持统一单价，无阶梯切换。无供应商请求。
- 隔离 PostgreSQL + 官方 new-api + MySQL，真实成本保存→预览→确认→worker→数据库/运行核验→目录，共 4 个成功任务：1.6、重复1.6、1.8、回到1.6，只有 3 次表达式写入；重复确认幂等，未决写入为0。随后恢复仅测试实例的原始表达式，未调用生产或真实供应商。
- 当前 React 页面浅色/深色桌面与390px手机验收通过；统一目标、专属价、确认门、修改后重新预览及不完整响应拦截均通过，无页面错误或外部请求。
- 独立审查无阻断项。开发证据位于 `/tmp/llmroute-uniform-pricing-qa`，引擎与审查证据位于 `/tmp/llmroute-uniform-pricing-audit`。

## 生产发布（11:44 已完成）

- 运行源码 `4be57f99e8975ac8fff7b00bf3687f9b98f262bf` 已按 `dev → prod → VPS` 发布；镜像 `sha256:dcfd2cc626a1c1005bd3da964ec2e9aeb48177b8a635c7d9d905531cc4306ba3`，标签 `silkroadai-portal-portal:release-4be57f9`。后续文档提交不重建镜像。
- 新鲜备份与证据目录 `/opt/backups/silkroadai-portal/releases/uniform-pricing-20260917-033418/`，环境备份 `.env.bak.uniform-pricing-20260917-033418`；PostgreSQL 备份 665,263 字节，0600、gzip 与真实隔离恢复通过。回滚镜像标签 `rollback-uniform-pricing-20260917-033418`，旧运行源码 bf312e6、旧 Git f7030a3。
- 生产数据副本26项检查通过，含真实认证页面、新版资源、既有 GPT 草稿只读预览、独立模型保存/历史/签名预览。临时容器、数据库和环境文件已清理。生产80条migration仅执行status核对，无DDL。
- 切换北京时间11:44:16–11:44:31，25个探测样本中13次失败，失败首尾跨度7.623秒；随后稳定，Portal restart0、启动日志error0，PostgreSQL/new-api/MySQL身份及启动时间不变。
- 50/50网络检查、14/14持久授权与用户计费模式检查通过；两类UA真实Key模型列表200/4模型，目录价格3项核对0差异，固定图片¥1/¥1.5/¥2不变，拓扑异常全0。五个线上价格页JS资源SHA均与镜像一致。
- 以本次发布前的新鲜备份为基准，31模型、43价格版本、6分组、24Key、1成本规则/历史，以及另外相关审计表共11表摘要全部保持；既有客户权限、8项存在的new-api配置原始字节和4项缺失状态均不变。
- 生产只做4次GET及1次POST `action=preview`：原草稿revision1不变；统一目标输入0.8/输出4.8/缓存读取0.08，专属配置0.9/5.4/0.09，均不限输入长度。`unchanged=false`准确表示目标将替换当前长度规则。发布jobs/writes仍0，无实际改价、撤销Key、收费生成或支付。
- 本次隔离fixture重建首轮公钥多一个换行导致NUL内嵌，应用严格PEM校验正确拒绝；修复仅涉及合成公钥文件，随后真实7项只读读取及完整生命周期通过，没有放宽应用校验。新发布helper另修正继承的旧79条迁移断言及V4漏检，38项语法和72项模拟检查通过。

回滚不能撤销已发布的价格。旧 bf312e6不认识V4任务；只要存在V4任务、未知版本或未决写入，不能启动旧worker。自动回滚门还保守要求完整价格元数据为空；不得删除业务记录强行通过。部署本身不确认任何价格发布。

操作：刷新定价页，保留原有充值比例和两项倍率，点击「预览此价格」；核对统一客户单价，确认后提交任务，等待「已生效」。旧预览已失效，不能直接复用旧页面确认。
# 发布预览简化补充（2026-09-17，14:52 已上线）

用户要求去掉不同输入长度的旧价格对比。应用提交 `d53fd79a4c494409143ce1eb5ffc9da00b543c5b`
只修改 `CostPricingWorkbench` 展示及对应既有测试：统一价格预览删除旧阶梯折叠区，普通与专属客户
均直接展示「发布后售价」，隐藏未配置的缓存写入空行。真实零价继续显示；专属客户价格、受影响
分组、确认和完整性校验保留。后端计费、成本草稿、价格历史与发布任务语义未改。

- 完整测试 `330 passed / 1 skipped` 文件、`4983 passed / 2 skipped` 用例；类型、构建、格式通过，
  lint 0 error / 93 个既有 warning。构建中的本机 PostgreSQL 不可达触发原价格页降级，生产只读
  页面验收另行通过。浅色、深色和 390px 手机真实组件检查均通过，确认门及缺失数据保护正常。
- dev → prod fast-forward 后，VPS 仅从 prod 构建并切换 Portal。运行镜像
  `sha256:30abb512cf3e2b4ba7d4a5a71c807faa158178729bb7a904b6b04352ddea4033`。
  北京时间 14:51:53–14:52:08 切换，26 次采样中 14 次失败，失败样本首尾跨度 8.126 秒后稳定；
  Portal restart 0、启动 error 0，PostgreSQL/new-api/MySQL 容器身份与启动时间不变。
- 新鲜备份与证据目录：`/opt/backups/silkroadai-portal/releases/pricing-simple-20260917-064404/`；
  环境备份 `.env.bak.pricing-simple-20260917-064404`，回滚镜像标签
  `silkroadai-portal-portal:rollback-pricing-simple-20260917-064404` 对应原统一计价版本 4be57f9。
  备份实际恢复至临时库并核对 11 张业务表、80 条 migration checksum 后删除临时库；新镜像不含
  `.env*`，Prisma 文件由运行用户可读。无 migration、环境、依赖、Nginx 或 Cloudflare 变更。
- 50/50 源站与公网检查、持久鉴权、后台成本 GET、业务摘要与页面入口检查通过。公网定价页 JS
  与新镜像 SHA256 一致，包含「发布后售价」，不含「查看当前价格对比」。11 张业务表、客户档次
  权限和 12 项 new-api 配置存在性/原始字节核对保持；jobs/writes 仍为 0，没有确认改价或收费调用。
- 本地证据 `/tmp/llmroute-pricing-simple-qa/`；服务器本轮干净构建目录已清理，备份和回滚镜像保留。
  本节后的文档归档提交不重建镜像。
