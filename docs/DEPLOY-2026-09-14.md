# 2026-09-14 上游同步生产发布

## 结果与版本

operator 明确「部署到线上吧」后，已将验收通过的 `prod@4413e615329279d99fea5bec9c8b03ed87d38fdc` 在 VPS 发布。北京时间 **10:53:24–10:53:39** 切换；0.5 秒间隔探测共 26 次，14 次失败，首次失败至首次恢复 HTTP 200 为 **9.425 秒**。这只是本机源站采样的中断窗口，不等于逐个客户请求的实测影响。

- 新运行镜像：`sha256:6c8d7e47ec47f11a7e525fd8fc4aab729c7b01dee6f337c8434b1ba671237b58`，标签 `silkroadai-portal-portal:release-4413e61`。
- Portal 容器：`7ae694140f833ac168bc2f221733f91775a63277157d2895a9218a6931a4f6c8`，启动于 `2026-09-14T02:53:25.461233152Z`，restart count 0。
- 应用自定义修复为 `e54cd75`，`4413e61` 包含最终验收记录；本次发布后的纯文档提交不重建镜像。
- `main` 保持上游 `3f6b407`，自定义提交在 dev 集成、测试后 fast-forward 到 prod；VPS 只拉取 prod。

本批 #459–#464 的逐提交北京时间、行为变化、冲突语义与负对照见 [上游同步报告](UPSTREAM-SYNC-2026-09-14.md)。主要包含视频失败状态识别、比例省略、图片适配器超时及质量边界，以及 Enterprise 迟到失败/过期不能覆盖已完成或已计费记录的条件更新。逐档价格操作与历史、动态拓扑、持久 token、固定图片 SKU 均保留。

## 备份、构建与切换

发布目录 `/opt/backups/silkroadai-portal/releases/upstream-20260914-022302/` 权限 0700，证据与备份文件 0600：

| 项目            | 实际结果                                                                                      |
| --------------- | --------------------------------------------------------------------------------------------- |
| PostgreSQL 备份 | 发布目录 `portal.sql.gz`，657146 bytes；dump 退出码与 gzip 完整性通过，并实际恢复到临时隔离库 |
| 环境备份        | `/opt/silkroadai-portal/.env.bak.upstream-20260914-022302`；实际 `.env` 切换前后 SHA-256 一致 |
| 其他备份        | Nginx 配置、new-api 九项价格/计费选项中实际存在的值、目录与 Key 摘要、迁移清单、旧实例身份    |
| 旧 Git          | `b092641b6a9004de373cd2d0ce3230d68857c9a9`，实际旧应用为 9 月 12 日逐档历史版                 |
| 旧运行镜像      | `sha256:585d8123d1a71e5c98ff58459b0be3214dc243ddb959b3c6adaa86c4ffcd62c2`                     |
| 回滚标签        | `silkroadai-portal-portal:rollback-upstream-20260914-022302`                                  |

10:24:28–10:28:10 在 VPS 从精确 Git 归档构建。构建目录排除 `.env*` 和运行时目录，公开 build args 取生产 Compose；非 root 镜像检查无 `/app/.env*`。旧服务在构建和隔离验收期间持续运行。无新增 migration、环境变量、依赖、Nginx 或 Cloudflare 配置变更。

候选使用本次备份恢复的随机临时数据库、独立容器和随机管理员/JWT 凭据；没有发布主机端口，调度器关闭，直接启动 Node。77 条 migration 全部完成且 checksum 与发布源代码一致；实际价格页面与 Kimi 发布预览通过，14 项候选检查成功，另确认三类临时资源清理完成。预览执行 new-api API 与只读 MySQL 四字典核对，没有发布任务、写入意图或价格变更。临时容器、临时数据库和含密环境文件全部清理并核验消失。

首轮候选检查错误要求价格页包含 `gpt-5.4`，得到 HTTP 200、无降级横幅但检查失败。只读确认生产该模型已下架，旧版页面同样不显示；保留首轮失败记录，将条件修正为当前在售的 `kimi-k3` 与 `gpt-image-2-1k`，再完整重跑候选检查。没有修改业务页面或下架状态，也没有把“未显示下架模型”当作发布故障。

切换前二次检查非终态价格任务、活动协调任务、in_flight 写入、未结算视频和活动 Batch 均为 0，并采样三次无现有 3002 连接；这不是原子停写机制。使用生产 Compose 的 `up -d --no-deps --no-build portal` 只替换 Portal，没有启用可选 profile。PostgreSQL、new-api、MySQL 的容器 ID、镜像、启动时间均与备份前一致。回滚预案保留旧镜像，并检查未决价格写入后才允许启动旧应用；本次没有触发回滚，不能称已经实测本次回滚。

## 上线后实际验收

| 检查           | 结果                                                                                                                             |
| -------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| 运行状态       | 四个预期容器 running，PostgreSQL healthy，Portal restart count 0；成功读取启动日志，error/fatal/failed 为 0                      |
| 数据库         | 77 applied，0 unfinished，0 rolled back，checksum 全匹配；本轮没有增量迁移                                                       |
| 数据保持       | 29 模型、35 价格版本、6 档次、21 Key 的全表摘要与切换前一致；发布 jobs/writes 仍为 0                                             |
| 计费与拓扑     | 全局及全部 13 用户均为 newapi 计费；9 项拓扑错误计数全部为 0                                                                     |
| 持久授权       | 当前 13/13 活动用户均为非 JWT 持久 token；真实 GET `/api/user/self` 身份匹配，无轮换，凭据前后不变                               |
| 公网模型列表   | 管理员自己已有 Key，以普通 UA 与 Python-urllib UA 分别 GET，均为 200；两次同为 4 模型，档次/元数据一致，3 项现行价格核对无差异   |
| 固定图片 SKU   | Portal 与 new-api 实际配置换算均为 1K ¥1、2K ¥1.5、4K ¥2                                                                         |
| 价格双读       | ModelRatio、CompletionRatio、ModelPrice、GroupRatio：API 前后稳定且与 MySQL 一致；四字典及额外计费选项部署前后未变               |
| 只读权限       | 既有账号仍仅 global USAGE 与 SELECT `new_api.options`；固定 RSA 公钥 hash 一致、uid 1001 可读；不把私有 Docker 网络 SQL 误称 TLS |
| 真实生产价格页 | HTTP 200，含当前在售文本及固定图片 SKU，无数据库降级横幅                                                                         |
| 真实生产预览   | Kimi 单次 `action=preview` 成功、影响 1 档，保留 3 条预览警示；没有 confirm/publish、任务或 PUT，价格/目录不变                   |
| 默认关闭功能   | Batch 与 Image 2.5 环境开关保持显式 false；本机入口实际返回 503 及各自预期错误码                                                 |
| 源站与公网     | 50/50 检查首次全部通过，详见下文                                                                                                 |

50 项网络检查包括 apex/www 登录页、模型页和价格页 200；API 假 Key 401、管理路径 404；四类内部适配器在 apex/www/API 源站及公网全部 404；OpenAI/Gemini CORS 204；Google/GitHub OAuth start 302；主站与 API access log 不记录测试 query string。Gemini 仍仅按无 Cookie 的 `x-goog-api-key` 模式验收，没有扩大凭据模式。

部署前后另使用 PostgreSQL 只读事务的 SHA-256 摘要，以及已有专用 MySQL reader 做独立比较：六张目录/Key/发布表、迁移、计费、拓扑、功能开关、固定图价、只读权限和价格选项均一致。历史记录曾有 12 用户；本次上线前已查到 13 活动用户，因此按当前事实验证 13，未将历史数量当作强制业务约束。

## 范围与留存

本批开发门禁已完成：完整测试 309 files / 4177 passed / 1 既有 skipped（含真实只读 smoke），typecheck、两套 Prisma validate、生产构建通过，lint 0 error / 93 既有 warnings。本次服务器构建和真实数据库价格页验收补齐了本地 PostgreSQL 缺失时的页面验收边界。

本次没有真实改价、收费模型生成、支付、完整浏览器 OAuth 登录，也未配置或启用新 provider。Image 2.5/Batch 仍关闭；新图片线路质量、收费及 300 秒响应头超时之后的 body 超时限制仍以同步报告为准。模型列表成功不能代替逐模型推理成功，预览成功不能代替真实价格发布完成。

发布证据保存在上述服务器发布目录；本机脚本、首轮候选失败、最终 JSON 及比较结果保存在 `/tmp/llmroute-deploy-20260914/`。用户既有 `.env`、`.idea/vcs.xml` 与未跟踪需求文档保持原样，未纳入提交。
