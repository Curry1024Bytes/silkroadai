# 2026-09-11 生产发布记录

operator 明确「好的，开始上线」后，按 `dev → prod → VPS` 发布。运行代码为 `e69f225a63b25460c20738e7bb69f133c35d49f4`，包括渠道替换预览功能与上游 #452–#458 集成修复。北京时间 10:54 切换成功；后续文档提交只同步发布记录，不重建镜像。

## 发布结果

| 项目                 | 已核实结果                                                                    |
| -------------------- | ----------------------------------------------------------------------------- |
| 服务器               | `82.29.71.122`，`/opt/silkroadai-portal`，仅从 prod 拉取                      |
| 发布镜像             | `silkroadai-portal-portal:release-e69f225`                                    |
| 镜像 ID              | `sha256:1d607b258ab6826d8fafadcc6b6ac6e241650bec7ed79eed09103a3332cfafe1`     |
| 切换时间（北京时间） | 10:54:05 开始，10:54:18 完成启动与初步稳定检查                                |
| 可用性               | 本机约每 0.5 秒探测，18 次采样中 11 次失败，约 6 秒短暂中断后恢复             |
| Portal               | 运行目标镜像，restart count 0，登录页面 200                                   |
| 依赖容器             | PostgreSQL healthy；PostgreSQL、new-api、MySQL 的 ID 与启动时间均未改变       |
| 数据库               | 76 applied、0 unfinished、0 rolled back；全部 checksum 匹配，无新增 migration |
| 配置                 | 生产 `.env` 摘要与发布前一致；Nginx/Cloudflare/网络/可选 profiles 未改        |
| 新功能开关           | Image 2.5、Batch 运行时均为 false，HTTP 关闭行为已核实                        |
| 日志                 | 10:55:14 检查启动后 24 行日志，0 error/fatal/failed                           |

切换前 Portal 的 3002 入站 established 连接为 0，未结算视频任务与 Batch 任务均为 0。Kuaizi 平台 Key 未设置。本次没有启用新供应商，也没有重启依赖容器或修改 new-api 源码、渠道与价格。

## 备份与构建

- `.env`：`/opt/silkroadai-portal/.env.bak.20260911-024813`，0600。
- PostgreSQL：`/opt/backups/silkroadai-portal/portal-20260911-024813.sql.gz`，约 644 KB、0600，`gzip -t` 通过。
- Nginx：`/opt/backups/silkroadai-portal/releases/20260911-024813/nginx-config.tar.gz`，0600，归档完整。
- 回滚 Git 状态：`65a99e5b4ff7d16b6c8001869ba2d15832b895c6`；其实际运行应用为 9/9 发布的 `d3c60d3`。
- 回滚镜像：`silkroadai-portal-portal:rollback-20260911-024813`，旧镜像 ID 为 `sha256:04126eede7e98d51848700b95a9242170583d2f48efb384e4c51efdf2f9572e5`。

北京时间 10:48:46–10:52:24，在旧 Portal 运行期间完成构建。使用当前 prod 的 Git 归档作为干净上下文，排除全部 `.env*` 与服务器备份；公开 build args 继续来自原生产 Compose。本次没有修改生产 `.env`。

一次性检查容器确认 UID 1001、76 条 migration SQL 全部可读、镜像 `/app` 没有 `.env*` 文件、两个功能开关均为 false。新镜像在生产 PostgreSQL 16 上执行只读 `prisma migrate status`，结果为数据库已是最新；没有手工执行迁移写入。Portal 启动时原有 `migrate deploy` 仍正常运行，没有待应用迁移。

Nginx 的两个生产配置与目标仓库模板一致，语法及 12 项源站内部路径隔离通过，无需安装或 reload。首次检查脚本误用了 API 配置文件名 `api.llmroute.club.conf`，发现文件不存在后按服务器实际文件 `llmroute-api.conf` 修正并重跑成功；没有修改 Nginx 配置，原检查日志保留。

## 上线验收

- **50 项网络检查通过**：apex/www 的登录、模型、价格页面源站与公网均 200；API 假 Key 401，API 登录/管理路径 404；四类内部适配器在三个域名的源站/公网全部 POST 404；两类 CORS 204；web/API 日志不含测试 query marker。
- Google/GitHub start 为 302，并指向各自官方域名；本次未重复真实浏览器登录。
- 12/12 活动用户的持久 token 在发布前后均为非 JWT，`GET /api/user/self` 返回匹配的用户 ID；没有轮换 token。
- 管理员自己的现有客户 Key 经公网 `GET /v1/models` 返回 200、4 个模型。默认 Python-urllib 同样返回 200，之前的 Cloudflare 客户端兼容修复仍有效；本次没有调整 Cloudflare，也未重新执行官方 SDK 或收费推理测试。
- 用户计费模式、空渠道、默认档、重复 group/channel、活动 Key 悬空档次、启用模型无 live route 等八项异常计数均为 0。固定图片价格仍为 1k ¥1、2k ¥1.5、4k ¥2。
- Image 2.5 generations/edits 为 `503/upstream_unavailable` 且无 usage；公网 Batch/files 为 `503/batch_unavailable`，Batch 任务数为 0。其余三个内部适配器无认证请求为 401。
- Portal → new-api 为 200 JSON；Nginx、sshd、fail2ban 均 active，SSH key-only 未变。

### 渠道替换功能

生产新接口的真实验证仅做元数据读取和默认 dryRun 预览：

1. 未鉴权 GET 返回 401；管理员 GET 返回 200，渠道对象仅含白名单字段，不含渠道密钥。
2. 使用当前 CCMax 的渠道 14 预览不匹配的渠道 6，正确列出 10 个受影响模型，返回不可确认；标明已有归属、group 不符、拓扑与缺失模型问题，并产生预览指纹。
3. 预览前后 `channel_groups`、`catalog_models`、`catalog_prices`、`newapi_tokens` 的内容摘要一致。未发送 `dryRun=false`，没有执行正式替换。
4. 已有 9→14 迁移保持原状：CCMax 登记 `[14]`，旧渠道 9 引用为 0，渠道 14 引用为 10。

正式替换事务的失败回滚、过期指纹、重复提交与租户隔离沿用已通过的回归；本次没有对生产数据库注入写入失败。浏览器交互沿用开发阶段的桌面、窄屏、主题和异常流程验证，没有把 API 预览写成新的浏览器验收。

## 验证边界与回滚

发布门为同一应用代码的完整测试 290 files / 3736 passed / 1 既有 skipped，原联机 smoke 3/3、类型与生产构建通过，lint 0 error / 93 既有 warnings。此次服务器实际构建使用 Node 22.23.2，也完成镜像启动与生产只读检查。

本次没有收费生成、支付、创建/删除客户 Key、实际渠道替换或新供应商验收。Image 2.5 / Batch 的待办继续有效；模型列表成功不等于各模型账号池和推理正常。

如需回滚，保留数据库与已保存镜像，优先在 dev 修复后按同样流程发布。若未来启用视频供应商并产生 `cgt-recovery-` 任务，恢复旧镜像前必须保留解码兼容直到这些任务终态，不能删除任务映射或恢复覆盖生产数据库。本次发布没有产生此类任务。

服务器证据目录为 `/opt/backups/silkroadai-portal/releases/20260911-024813/`；本机证据为 `/tmp/llmroute-deploy-20260911/`（0700），含原始检查、构建、切换采样和验收日志。用户本机 `.env` 与未跟踪需求文档按开工前 SHA-256 核对，未修改或提交。
