# W8 D8 — Deploy 记录(2026-06-04)

> Prod 部署:`bc1d5e4` (#69) → `840e132` (#72)。3 个 commit 上线。
> 授权:operator「开 PR = deploy」,直接 push + merge + SSH deploy。

---

## 1. 本次 deploy 包含的变更

prod 部署前已在 `bc1d5e4`(PR #69,executeRecharge numeric overflow 修复 + 其 migration 已 apply)。本次 `git pull` fast-forward 拉入 3 个 commit:

| PR  | commit    | 说明                                                                       |
| --- | --------- | -------------------------------------------------------------------------- |
| #70 | `ec4b86f` | chore(reseller): commission audit(无 bug)+ backfill 工具(纯 docs + script) |
| #71 | `315fe29` | **fix(image): Gemini Channel 17 走 native endpoint 真出 2K/4K**            |
| #72 | `840e132` | docs(landing): customer API integration guide(/docs 加 4 章)               |

> 注:#69(numeric overflow 修复)在本次之前就已部署(prod 24h 前已在 #69),其 migration `20260603153145_w8_d8_widen_recharge_log_numeric` 早已 apply。本次三个 commit **不含任何新 migration**。

### 1.1 PR #71 的核心(本次主角)

portal `/image` 生图对 2K/4K Gemini 模型(`gemini-3.1-flash-image-preview` → 2K,`gemini-3-pro-image-preview` / `nano-banana-pro-preview` → 4K)改走 **native** `POST /v1beta/models/{model}:generateContent` + `generationConfig.imageConfig.imageSize`。这是唯一能让上游(Channel 17 = nexaxis)真出 2K/4K 的请求形;OpenAI 兼容的 `/v1/chat/completions` 会静默丢弃分辨率提示、只给 1K。不影响 gpt-image-2 / Imagen / 1K Nano Banana。详见 `docs/W8-D8-NEXAXIS-IMAGE-RESOLUTION.md`。

---

## 2. Migration drift 检查(deploy 前,硬性门)

```
SELECT count(*) FROM _prisma_migrations WHERE migration_name LIKE '%pr_g2%';
→ 0   ✅(无 drift,放行)
```

`20260603153145_w8_d8_widen_recharge_log_numeric` 已 apply(2026-06-03 16:15)。deploy 时 `prisma migrate deploy` 输出 **No pending migrations to apply** —— 干净 no-op(本次 3 个 commit 不含新 migration)。

---

## 3. Deploy 执行

```
cd /opt/silkroadai-portal
git pull origin main        # Fast-forward bc1d5e4..840e132,8 files,无冲突
docker compose -f docker-compose.prod.yml up -d --build portal
```

- docker build:成功(image `silkroadai-portal-portal:latest` 重建,无 build error)。
- 容器:`silkroadai-portal` 重建并启动;`silkroadai-portal-db` healthy。
- 启动日志:`No pending migrations to apply` → `Migrations complete` → `✓ Ready in 273ms` → `Order timeout scheduler started`。

---

## 4. Smoke test 结果(全过)

| #   | 检查                                                                              | 结果                                                                                                                 |
| --- | --------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| 1   | `GET /docs`                                                                       | **200** ✅                                                                                                           |
| 2   | `GET /image`                                                                      | **307**(未登录重定向 /login,符合预期)✅                                                                              |
| 3   | `/docs` 含新章节                                                                  | ✅ 新 section id(`api-endpoints` / `api-image` 等)+ `imageSize` / `generateContent` 均在渲染 HTML(brief 正则 5 命中) |
| 4   | `POST /v1/chat/completions`(gpt-5.4)                                              | ✅ 返回正常 JSON chunk(非 502/500)—— #69 executeRecharge 改动未破坏 chat                                             |
| 5   | **`POST /v1beta/...gemini-3.1-flash-image-preview:generateContent` imageSize=2K** | ✅ **解码图像 2048×2048**(6.98 MB)—— fix #71 上线生效                                                                |

smoke #5 是关键验证:确认 portal 改动已生效,客户走 native endpoint 真拿 2K。

---

## 5. 24h 监控

部署后日志即时扫描(`numeric` / `overflow` / `NEEDS_REVIEW` / `nexaxis` / `429` / `Error`):**0 条**。

后台 watcher 已起(VPS,自动 24h 后停):

```
tail -f /tmp/portal-watch-w8d8.log      # 在 vps 上,匹配上述模式才追加
```

24h 后人工复核要点:

- numeric overflow:期望 0 条(#69 已在真实充值流量下跑了 24h+,目前 0)
- NEEDS_REVIEW:期望 0 条
- nexaxis 429:>0 属正常(4K 库存每日有限,会以干净 429 抛给客户、不扣费、不降级);若高频则考虑联系 nexaxis 或加 Gemini 上游分摊

---

## 6. 注意事项 / 已知项

- **main CI 长期红(预存,非本次引入)**:`Format`(11 个历史文件未 prettier)+ `Test`(`landing-page.test.tsx` 的 `¥1.20/1M` / `¥4.5/1M` 定价断言,自 #67 起红)。`Type Check` / `Lint` 一直绿。#71 / #72 本身在 Type Check / Lint / 自身单测全绿(各自只引入干净文件),团队历来 merge-through-red。建议后续单独清理这两项。
- 本次未改 `ModelPrice` / `ModelRatio` / Channel 17 config / 客户余额。
- operator 在途未提交的 WIP(reseller / 定价 / 时区那批 M 文件)全程未触碰。
