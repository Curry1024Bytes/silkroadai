# LLmRoute Portal 生产部署与运维手册

> **当前生产环境唯一权威手册**
>
> 最后核对:2026-09-15(Asia/Shanghai)，最新发布见文末记录。
>
> 适用环境:`llmroute.club` / AlmaLinux 9.7 / Nginx / Cloudflare / Docker
>
> 历史环境和历史验收文档只用于审计,不能替代本文执行当前生产操作。

本文不记录任何密码、OAuth Client Secret、支付密钥或访问令牌。真实密钥只保存在服务器
`/opt/silkroadai-portal/.env`、`/root/new-api.env` 和 operator 的密码管理器中。

---

## 1. 分支与发布规则

三个长期分支的职责固定如下:

| 分支   | 职责       | 允许的操作                                           |
| ------ | ---------- | ---------------------------------------------------- |
| `main` | 上游镜像   | 只同步上游,不做项目自定义修改,不解决项目冲突,不部署  |
| `dev`  | 开发与集成 | 自研开发、合并 `main`、解决冲突、类型检查和完整测试  |
| `prod` | 生产发布   | 只接收测试通过的 `dev` fast-forward,VPS 只部署此分支 |

每次同步上游必须先报告、后执行。报告包括每个提交的北京时间、SHA、行为变化、migration、
环境变量、服务器/Nginx/Cloudflare/安全影响,以及与 `dev` 的差异和冲突预判。固定流程是:

```text
upstream -> main -> 变更报告 -> dev 合并/解决冲突 -> 完整验证
         -> prod fast-forward -> operator 明确安排 -> VPS 手动部署
```

CI 只做检查,不会自动部署。禁止从 `main` 直接上线。

2026-09-15 operator 已授权「做好并测试通过后直接上线」。对已安排的工作，该持续授权满足流程中的发布许可：检查通过后由 Codex 按本手册直接执行备份、迁移和部署，无须再次确认。后续明确的暂停或先沟通指令优先；实际改价、收费调用及未安排的改动不在此授权内。

---

## 2. 当前生产快照

### 2.1 服务器与入口

| 项目           | 当前值                                                |
| -------------- | ----------------------------------------------------- |
| 公网 IP        | `82.29.71.122`                                        |
| SSH            | `root@82.29.71.122:22`;仅公钥,密码登录已关闭          |
| 操作系统       | AlmaLinux 9.7                                         |
| 时区           | `Asia/Shanghai`                                       |
| Portal 目录    | `/opt/silkroadai-portal`                              |
| Portal 分支    | `prod`                                                |
| 反向代理       | 宿主机 Nginx(systemd),不是 Docker Nginx               |
| DNS/CDN        | Cloudflare 橙云代理                                   |
| Cloudflare SSL | Full (strict)                                         |
| 公开主站       | `https://llmroute.club`、`https://www.llmroute.club`  |
| 客户 API 域名  | `api.llmroute.club`;API-only Nginx 路由已部署并验收   |
| 图片域名       | `images.llmroute.club` 尚未完成平台 R2 配置与公网验收 |

架构现状:

```text
Browser
  -> Cloudflare(Full strict)
  -> host Nginx :443
  -> 127.0.0.1:3002
  -> silkroadai-portal
       -> portal-postgres:5432
       -> host.docker.internal:3000
          -> host 172.17.0.1:3000
          -> new-api -> new-api-mysql
```

### 2.2 当前容器和网络

| 容器                   | 当前用途             | 宿主机绑定        | 启动方式                                            |
| ---------------------- | -------------------- | ----------------- | --------------------------------------------------- |
| `silkroadai-portal`    | Next.js Portal       | `127.0.0.1:3002`  | `docker-compose.prod.yml` 的 `portal` 服务          |
| `silkroadai-portal-db` | Portal PostgreSQL 16 | `127.0.0.1:5432`  | `docker-compose.prod.yml` 的 `portal-postgres` 服务 |
| `new-api`              | 模型路由与计费       | `172.17.0.1:3000` | 手工 `docker run`                                   |
| `new-api-mysql`        | new-api MySQL 8      | 不发布宿主机端口  | new-api 原有 Docker 网络                            |

关键网络事实:

- new-api 和 MySQL 所在网络:`new-api_new-api-net`。
- Portal 的 compose 也会加入这个 external network,部署前该网络必须存在。
- Portal 仍通过 `NEWAPI_BASE_URL=http://host.docker.internal:3000` 访问 new-api。
- new-api 必须绑定 `172.17.0.1:3000`,不能绑 `127.0.0.1:3000`,否则 Portal 容器访问会被拒绝。
- 不要把 new-api 改成 `0.0.0.0:3000`,否则会扩大公网暴露面。
- new-api 持久化目录:`/opt/new-api/data -> /data`。
- 服务器上可能保留停止状态的 `new-api-old-port` 回退容器。确认新容器和备份稳定前不要误删。

`docker-compose.prod.yml` 还包含上游的 `portal-api-1..3` 和 `seedance-portal`,但已经用 Compose
profiles 隔离:

- `portal-api-1..3` 属于 `api-replicas` profile。
- `seedance-portal` 属于 `seedance` profile。
- 默认不启用 profile 时,只会启动主站 Portal 及其 PostgreSQL 依赖。

这台服务器当前只部署主站 Portal,日常发布仍显式指定 `portal`,让操作目标一眼可见:

```bash
docker compose -f docker-compose.prod.yml up -d portal
```

如未来明确部署上游可选服务,必须显式启用对应 profile,例如
`docker compose -f docker-compose.prod.yml --profile api-replicas up -d`。不要在当前 LLmRoute
服务器启用未配置、未验收的 profile。

### 2.3 已验证与未完成

截至 2026-08-04 已在真实生产环境验证:

- `https://llmroute.club/login` 和 `https://www.llmroute.club/login` 返回 200。
- Google OAuth 与 GitHub OAuth 真实浏览器登录成功,并能完成 new-api 用户开户。
- zpayz 支付宝真实充值成功,异步回调后余额正确且只增加一次。
- `api.llmroute.club` 已使用独立 API-only Nginx virtual host;`/v1/models` 对假 Key 返回 401,
  `/v1/*` 与 `/v1beta/*` CORS 预检返回 204,`/login` 和 `/admin/login` 返回 JSON 404。
- 主站 `/image-adapter/*` 对公网返回 JSON 404;Docker 内部以服务名访问适配器时,缺少 Authorization
  返回预期 401。API access log 已确认不会记录 Gemini `?key=` 查询参数。
- Portal 容器能访问 `host.docker.internal:3000/api/setup` 并得到 200 JSON。
- Nginx Origin Certificate 与 Private Key 匹配,SAN 包含 `llmroute.club` 和 `*.llmroute.club`。
- Cloudflare Cache Rule `Bypass LLmRoute API` 已启用,条件为 `http.host eq "api.llmroute.club"`。
- 生产 `.env` 已补齐 `NEXT_PUBLIC_API_URL`、`NEXT_PUBLIC_IMAGE_URL`、`NEXT_PUBLIC_SUPPORT_WECHAT` 和
  `NEWAPI_CUSTOMER_BASE_URL=http://127.0.0.1:3002`。
- SSH 已完成 key-only 加固并用原生 OpenSSH、Xterminal 新会话验证;root 密码登录已关闭。`fail2ban`
  的 `sshd` jail 已启用并实际封禁攻击 IP。
- VPS 已启用并持久化 TCP BBR(`tcp_bbr` + `fq`),用于缓解 VPS 到中国电信网络约 17%-28% 下行重传
  导致的 CUBIC 拥塞窗口塌缩;Mac 上的 new-api SSH 隧道与完整后台页面已重新验收。
- Portal PostgreSQL 本机备份 cron 已启用:每日北京时间 02:00 执行,保留 7 天;已在 cron 最小环境中
  手工执行并验证备份为 0600、`gzip -t` 成功,日志和 logrotate 配置正常。
- 最新备份已恢复到同一 PostgreSQL 容器内的临时隔离数据库:生产/恢复均为 40 张表、60 条已完成
  migration,恢复库 5 个用户可读;验证后临时数据库已删除,生产库未被修改。
- 首份手工异机副本已通过 SSH 保存到 operator Mac 的 `~/Backups/LLmRoute/portal/`:文件权限 0600、
  `gzip -t` 成功且 Mac FileVault 已开启。2026-08-04 副本 SHA-256 为
  `266545000e6c7fe113429fc41b5da33ffd9ca9ba34239cc7c178336c8b6227aa`。

尚未完成或必须再次确认:

- SMTP 账号为空:邮箱验证、找回密码和余额提醒邮件不可用。余额提醒调度器会记录
  `SMTP_HOST / SMTP_USER / SMTP_PASS not configured`,但异常已被任务捕获,不影响 Portal 进程。
- 平台 R2/OSS 为空:生成图片的持久化公共存储未完成。
- Sentry 为空:线上异常没有集中告警。
- Tavily 为空:Chat UI 联网搜索开关不会产生搜索结果。
- new-api 尚未配置正式渠道和模型,所以真实客户 Key 的 `/v1` 模型调用仍待上货后验收。当前只完成
  API 路由、鉴权失败、CORS、SSE 配置和内部连通性验收,不能把它写成真实推理已通过。
- 当前 new-api 使用 MySQL,而上游 `NEWAPI_LOGS_DATABASE_URL` 直连导出实现只支持 PostgreSQL;
  因此变量应保持未设置,日志导出会使用最多 10,000 行的 API fallback。迁移数据库或补 MySQL 适配前不能填伪造连接串。
- 自动异机备份和跨主机恢复演练仍未完成;当前 Mac 手工副本不能替代持续、版本化的异地备份。
- zpayz 当前通道最终收款方是个人支付宝。收款主体由 zpayz 商户/渠道配置决定,不是 Portal 代码决定;
  企业支付宝和结算主体仍需在支付渠道侧办理。

### 2.4 2026-08-04 发布记录

| 项目             | 结果                                                           |
| ---------------- | -------------------------------------------------------------- |
| VPS 工作区 SHA   | `prod@149d9c4`                                                 |
| Portal 镜像 SHA  | `a166b28`;后续提交仅文档/备份脚本,未重建容器                   |
| 回滚 SHA         | `59410da`                                                      |
| 数据库备份       | `/opt/backups/silkroadai-portal/portal-20260804-030647.sql.gz` |
| migration        | 两个 forward migration 成功;无 rolled back                     |
| AK/SK backfill   | `secret_key_hash IS NULL` 为 0,无需执行                        |
| Portal/DB        | Portal running、restart count 0;PostgreSQL healthy             |
| Nginx/Cloudflare | web/API 模板已安装;源站与公网 smoke、CORS、日志脱敏均通过      |
| 真实业务         | Google、GitHub 登录与 zpayz 支付宝充值/余额入账通过            |
| SSH 安全         | root key-only;密码登录关闭;fail2ban sshd jail 已启用并命中     |
| 本机自动备份     | 每日 02:00 cron;0600、gzip、最小环境执行与 logrotate 均通过    |
| 本机恢复演练     | 临时库恢复成功;40 表、60 migration、5 用户可读;临时库已删除    |
| 手工异机副本     | Mac FileVault;0600、gzip、SHA-256 校验通过                     |
| 延后验收         | new-api 未上货,真实模型调用待渠道和模型配置完成后执行          |

发布前同时备份了生产 `.env` 和 `/etc/nginx/{nginx.conf,conf.d}`;备份文件权限均为 600。上线后日志只出现
已知 SMTP 空配置错误,没有 migration、OAuth、支付签名、new-api 连通或持续 5xx 错误。

### 2.5 2026-08-05 发布记录

| 项目         | 结果                                                                        |
| ------------ | --------------------------------------------------------------------------- |
| VPS/镜像 SHA | `prod@b480b09`                                                              |
| 回滚 SHA     | `149d9c4`                                                                   |
| `.env` 备份  | `.env.bak.20260805-114701`,0600                                             |
| 数据库备份   | `/opt/backups/silkroadai-portal/portal-20260805-034701.sql.gz`,0600/gzip OK |
| 发布内容     | 图片 usage 去重、ominiapi `n>1` 最多 10 路扇出、隐藏两项旧后台导航          |
| 发布门       | 245 files / 2687 passed / 1 skipped;typecheck/format/lint 0 error;build OK  |
| migration    | 60 条已应用,无 pending/rolled back                                          |
| Portal/DB    | Portal running、restart count 0;PostgreSQL healthy                          |
| 内部链路     | Portal -> `host.docker.internal:3000/api/setup` 返回 200                    |
| 公网 smoke   | apex/`www` 登录 200、API 假 Key 401、image-adapter 404、OAuth start 302     |
| 计费保护     | `BILLING_SOURCE` 未设置(默认 newapi);5 个用户全部 `billing_mode=newapi`     |
| 延后验收     | new-api 未上货;真实模型调用和图片扇出仍待配置渠道后验证                     |

该批次没有 migration、环境变量、依赖或 Nginx 变更。运行中的 Portal 先在旧镜像上完成构建,镜像构建成功后
才执行容器切换;new-api、MySQL 和 Portal PostgreSQL 未重建。

### 2.6 2026-08-06 发布记录

| 项目         | 结果                                                                        |
| ------------ | --------------------------------------------------------------------------- |
| VPS/镜像 SHA | `prod@53d30a3`                                                              |
| 回滚 SHA     | `b480b09`                                                                   |
| `.env` 备份  | `.env.bak.20260806-211143`,0600                                             |
| 数据库备份   | `/opt/backups/silkroadai-portal/portal-20260806-131143.sql.gz`,0600/gzip OK |
| 发布内容     | 日志降噪、Enterprise 480p/Ark 对齐、Responses 5xx 重试、素材统一平台托管    |
| 发布门       | 245 files / 2708 passed / 1 skipped;typecheck/format/lint 0 error;build OK  |
| migration    | 新增 2 条成功;累计 62 applied / 0 pending / 0 rolled back                   |
| Portal/DB    | Portal running、restart count 0;PostgreSQL healthy;5 个用户可读             |
| 内部链路     | 宿主机与 Portal 容器访问 new-api `/api/setup` 均为 200                      |
| 公网 smoke   | apex/`www` 200、API 假 Key 401、非 API/image-adapter 404、CORS 204          |
| OAuth        | Google/GitHub start 均 302 到官方域名;本次未重复真实浏览器登录              |
| R2           | Account/Access Key/Secret 未设置;Enterprise 素材上传未启用、未验收          |
| 延后验收     | 真实支付未重复执行;new-api 未上货,真实模型调用继续 pending                  |

该批次没有新环境变量、依赖或 Nginx/Cloudflare 变更。新增 migration 为
`20260806040000_add_seedance_task_submit_params` 和 `20260806120000_add_asset_group_type`,均由
`start.sh` 的 `prisma migrate deploy` 自动成功应用。素材组存量行通过默认值兼容,发布时数据库中没有
素材组数据。生产 Compose 只重建 Portal,new-api、MySQL 和 Portal PostgreSQL 均未重建。

### 2.7 2026-08-24 用户专属倍率与新用户默认 Key 发布记录

| 项目         | 结果                                                                                      |
| ------------ | ----------------------------------------------------------------------------------------- |
| VPS/镜像 SHA | `prod@8a9181f`                                                                            |
| 发布内容     | 用户专属倍率同步、计费单位校准、新用户首 Key 默认档修复、9 个历史错误分组 Key 迁移        |
| 计费参数     | `NEWAPI_QUOTA_PER_USD=500000`、`USD_TO_CNY_RATE=1`、`REAL_USD_TO_CNY_RATE=7.2`            |
| 专属倍率     | A 用户有效倍率 0.18;公共 `GPT-Pro20x(企业级)` 基础倍率仍为 0.20                           |
| 新用户首 Key | Portal `gpt特惠分组`;new-api Token.group `GPT-特惠反代`;缺失/重复默认档时返回 503         |
| migration    | `20260824231000_enforce_single_enabled_default_channel_group`;累计 69 applied / 0 pending |
| 数据备份     | `/opt/backups/new-user-default-key-20260824-233422`;Portal/new-api 备份完整性已验证       |
| 生产验收     | Portal restart count 0;主站 200;API 401/404;CORS 204;OAuth start 302;new-api 链路 200     |

首次切换容器时发生过约 19 分钟 Portal 中断。根因不是 migration 内容或数据库状态,而是人工备份时在当前
登录 shell 设置了 `umask 077`,随后同一 shell 执行 `git pull`;新 migration 目录成为 0700、SQL 成为
0600 且属主为 root。runner 以 `nextjs` 运行,无法读取文件,Prisma 因而报 `P3015 migration.sql missing`。
故障发生时 migration 尚未执行、Key 迁移也尚未开始;修复宿主机权限并以 `umask 022` 重建后部署成功,
没有数据丢失或半迁移。第 4.4 节的权限发布门和 Dockerfile 的 runner 权限兜底均不得删除。

---

## 3. 生产配置

### 3.1 文件规则

| 文件                 | 是否进 Git | 用途                      |
| -------------------- | ---------- | ------------------------- |
| `.env.local.example` | 是         | 本地开发模板              |
| `.env.prod.example`  | 是         | 生产配置模板,不含真实密钥 |
| `.env`               | 否         | 服务器真实生产配置        |

首次部署才执行:

```bash
cd /opt/silkroadai-portal
cp .env.prod.example .env
chmod 600 .env
vim .env
```

滚动更新时**绝不能用模板覆盖已有 `.env`**。拉代码不会覆盖被 Git ignore 的 `.env`,只需根据模板 diff
补新增变量。查看或编辑真实配置前先进入正确目录:

```bash
cd /opt/silkroadai-portal
pwd
ls -l .env .env.prod.example
vim .env
```

### 3.2 必填配置组

以下只列变量名和约束,不在文档写真实 secret:

- 数据库:`DATABASE_URL`、`POSTGRES_PASSWORD`,两处密码必须一致,host 必须是 `portal-postgres:5432`。
- new-api:`NEWAPI_BASE_URL`、`NEWAPI_CUSTOMER_BASE_URL`、`NEWAPI_ADMIN_TOKEN`、`NEWAPI_ADMIN_USER_ID`。
- Portal 认证:`PORTAL_JWT_SECRET`、`ADMIN_TOKEN`、`PORTAL_OSS_ENC_KEY`。
- 域名:`APP_URL`、`NEXT_PUBLIC_APP_URL`、`BRAND_COOKIE_DOMAIN`。
- OAuth:`GOOGLE_OAUTH_*`、`GITHUB_OAUTH_*`。
- 支付:`PAYMENT_PROVIDERS`、`ENABLED_PAYMENT_TYPES`、`EASY_PAY_*`。

生产固定公开值:

```env
NEWAPI_BASE_URL="http://host.docker.internal:3000"
# Portal 自己的 Chat/Image 服务端请求回到同容器 /v1 proxy,避免绕 Cloudflare 回源。
NEWAPI_CUSTOMER_BASE_URL="http://127.0.0.1:3002"
# new-api QuotaPerUnit 保持 500000;Portal 以 1 个技术计费单位 = ¥1 解释该额度。
NEWAPI_QUOTA_PER_USD=500000
USD_TO_CNY_RATE=1
# 只用于把人民币展示为真实美元,不参与充值和人民币扣费。
REAL_USD_TO_CNY_RATE=7.2
APP_URL="https://llmroute.club"
NEXT_PUBLIC_APP_URL="https://llmroute.club"
NEXT_PUBLIC_API_URL="https://api.llmroute.club"
NEXT_PUBLIC_IMAGE_URL="https://images.llmroute.club"
NEXT_PUBLIC_SUPPORT_EMAIL="support@llmroute.club"
BRAND_COOKIE_DOMAIN=".llmroute.club"
NEXT_PUBLIC_SUPPORT_WECHAT="LLmRoute"

GOOGLE_OAUTH_REDIRECT_URI="https://llmroute.club/api/auth/oauth/google/callback"
GITHUB_OAUTH_REDIRECT_URI="https://llmroute.club/api/auth/oauth/github/callback"

PAYMENT_PROVIDERS=easypay
ENABLED_PAYMENT_TYPES=alipay,wxpay
EASY_PAY_API_BASE="https://zpayz.cn"
EASY_PAY_NOTIFY_URL="https://llmroute.club/api/easy-pay/notify"
EASY_PAY_RETURN_URL="https://llmroute.club/pay/result"
PRODUCT_NAME="LLmRoute 余额充值"
```

Google/GitHub Client Secret、`EASY_PAY_PKEY` 和 `NEWAPI_ADMIN_TOKEN` 只能填服务器 `.env`,不得提交 Git,
也不要在聊天或终端截图中打印。

### 3.3 构建时变量

所有 `NEXT_PUBLIC_*` 都可能被 Next.js 编译进客户端产物。修改后必须重新 build,仅重启容器不可靠。
`docker-compose.prod.yml` 会从同目录 `.env` 读取以下公开变量,并作为 build args 传给 Dockerfile:

```env
APP_URL="https://llmroute.club"
NEXT_PUBLIC_APP_URL="https://llmroute.club"
NEXT_PUBLIC_API_URL="https://api.llmroute.club"
NEXT_PUBLIC_IMAGE_URL="https://images.llmroute.club"
NEXT_PUBLIC_SUPPORT_EMAIL="support@llmroute.club"
NEXT_PUBLIC_SUPPORT_WECHAT="LLmRoute"
```

Dockerfile 对这些变量也提供 LLmRoute 默认值,避免直接构建时回落到上游旧域名。标准生产构建不再需要逐个手写
`--build-arg`;先核对 `.env`,再执行:

```bash
docker compose -f docker-compose.prod.yml build portal
docker compose -f docker-compose.prod.yml up -d portal
```

纯运行时变量修改后可执行 `up -d portal`;只要改了代码或 `NEXT_PUBLIC_*`,就按上面的 build + up 两步执行。

### 3.4 不可轮换项

- `PORTAL_OSS_ENC_KEY` 用于解密客户已保存的 OSS Secret。投产后不能随意更换,否则历史配置无法解密。
- new-api 后台“系统访问令牌”的生成操作是 rotate。重新生成会立即让旧 `NEWAPI_ADMIN_TOKEN` 失效。
- 数据库密码修改需要同时处理 `.env`、PostgreSQL 用户和现有 volume,不能只改模板。

---

## 4. 标准发布流程

### 4.1 本地发布门

在 Mac 仓库执行:

```bash
cd /Users/elon/work/code/ai/silkroadai
git switch dev
git status --short
pnpm typecheck
pnpm lint
pnpm test:ci
pnpm format:check
```

确认 `dev` 包含要发布的内容且测试通过后,只允许 fast-forward `prod`:

```bash
git merge-base --is-ancestor prod dev
git switch prod
git merge --ff-only dev
git push origin prod
git switch dev
```

如果 `--ff-only` 失败,停止并分析分支历史,不要强推、不要重写 `main`。

### 4.2 登录服务器并做发布前检查

```bash
ssh -4 root@82.29.71.122
cd /opt/silkroadai-portal

git status --short
git rev-parse --abbrev-ref HEAD
git log -1 --oneline
docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'
docker network inspect new-api_new-api-net >/dev/null
```

要求:

- Git 工作区无未提交修改。
- 当前分支是 `prod`。
- `new-api`、`new-api-mysql`、Portal 和 Portal DB 都在运行。
- external network `new-api_new-api-net` 存在。
- `.env` 已备份且权限为 600。

```bash
cp -a .env ".env.bak.$(date +%Y%m%d-%H%M%S)"
chmod 600 .env
```

备份文件应靠脚本或 `chmod 600` 控制权限。若人工备份曾在当前登录 shell 执行 `umask 077`,备份结束后
必须执行 `umask 022`;不要在该 shell 保留 077 后继续 `git pull` 或构建镜像。`bash scripts/backup-db.sh`
在子 shell 内设置 077,不会污染当前 shell;禁止改成 `source scripts/backup-db.sh`。

### 4.3 备份 Portal 数据库

```bash
cd /opt/silkroadai-portal
bash scripts/backup-db.sh
ls -lh /opt/backups/silkroadai-portal/ | tail
gzip -t "$(ls -1t /opt/backups/silkroadai-portal/portal-*.sql.gz | head -1)"
```

备份脚本不对 `pg_dump` 分配 TTY,使用 0600 临时文件并在 `gzip -t` 成功后原子重命名;同一时间只允许
一个实例运行。每日北京时间 02:00 的 system cron 配置:

```cron
SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

0 2 * * * root /opt/silkroadai-portal/scripts/backup-db.sh >> /var/log/silkroadai-portal-backup.log 2>&1
```

cron 文件应保存为 `/etc/cron.d/silkroadai-portal-backup`,权限 0644。安装后必须手工执行一次脚本,确认
新备份为 0600、`gzip -t` 成功,再检查 `systemctl is-active crond`。脚本只保留本机 7 天;它不是异机
备份,生产稳定后还需补对象存储/异机副本和隔离数据库恢复演练。

### 4.4 拉取并部署

生产构建统一使用本节的 Git 归档上下文；本手册其他位置的 `build portal` 示例也须采用该方式，避免把服务器配置备份复制进镜像构建层。

```bash
cd /opt/silkroadai-portal
git switch prod

# 备份所需的 077 不得带入源码拉取。先恢复标准权限掩码并记录回滚点。
umask 022
test "$(umask)" = "0022"
DEPLOY_PREV_SHA=$(git rev-parse HEAD)

git pull --ff-only origin prod
git log -1 --oneline

# Prisma runner 是非 root 用户。修复并检查所有 schema/migration 对容器用户可读。
chmod -R a+rX prisma
test -z "$(find prisma -type d ! -perm -0555 -print -quit)"
test -z "$(find prisma -type f ! -perm -0444 -print -quit)"
git diff --name-status "$DEPLOY_PREV_SHA"..HEAD -- prisma/migrations prisma/schema.prisma

# 必须在 build 之前给线上旧镜像打回滚标签。VPS 用 containerd 镜像存储，旧镜像若只有
# latest 一个名字，build 把 latest 指向新镜像后旧镜像即不可寻址，事后 tag / commit 都会失败。
# 任一命令失败就停止发布，不要继续 build。
DEPLOY_TS=$(date -u +%Y%m%d-%H%M%S)
DEPLOY_ROLLBACK_TAG="silkroadai-portal-portal:rollback-$DEPLOY_TS"
DEPLOY_OLD_IMAGE=$(docker inspect -f '{{.Image}}' silkroadai-portal)
docker tag "$DEPLOY_OLD_IMAGE" "$DEPLOY_ROLLBACK_TAG"
test "$(docker image inspect -f '{{.Id}}' "$DEPLOY_ROLLBACK_TAG")" = "$DEPLOY_OLD_IMAGE"
echo "rollback: $DEPLOY_ROLLBACK_TAG -> $DEPLOY_OLD_IMAGE (git $DEPLOY_PREV_SHA)"

# 服务器的 .env.bak.* 等备份不能进入构建上下文；仅归档当前 prod 提交的源码。
# 公开 build args 仍由原生产 Compose 和同目录 .env 提供。
DEPLOY_CONTEXT_DIR=$(mktemp -d /tmp/llmroute-build.XXXXXX)
mkdir "$DEPLOY_CONTEXT_DIR/source"
git archive HEAD | tar -x --exclude=.env --exclude='.env.*' -C "$DEPLOY_CONTEXT_DIR/source"
chmod -R a+rX "$DEPLOY_CONTEXT_DIR/source/prisma"
python3 - "$DEPLOY_CONTEXT_DIR" <<'PY'
import json, pathlib, sys
p = pathlib.Path(sys.argv[1])
(p / 'context.json').write_text(json.dumps({
    'services': {'portal': {'build': {'context': str(p / 'source')}}}
}))
PY
docker compose -p silkroadai-portal -f docker-compose.prod.yml \
  -f "$DEPLOY_CONTEXT_DIR/context.json" build portal
# 新镜像同时打 release 标签，作为下一次发布的回滚点。
docker tag silkroadai-portal-portal:latest "silkroadai-portal-portal:release-$(git rev-parse --short HEAD)"

# 只启动一次性检查容器,不执行 start.sh、不连接数据库、不切换线上容器。
# 禁用交互 stdin，避免通过 ssh bash -s 发布时吞掉后续脚本。
docker compose -f docker-compose.prod.yml run --rm --no-deps -T --interactive=false --entrypoint sh portal -c \
  'test -r prisma/schema.prisma && find prisma/migrations -type f -name migration.sql -exec test -r {} \;'

docker compose -f docker-compose.prod.yml up -d portal
```

`start.sh` 会在容器启动时自动执行 `prisma migrate deploy`,然后启动 Next.js。不要手工修改 migration 历史,
也不要在生产库执行 `prisma migrate reset`。`chmod -R a+rX prisma` 只增加读取/目录遍历权限,不会修改 SQL
内容;Dockerfile 还会把 runner 中的 Prisma 文件交给 `nextjs:nodejs` 并再次保证可读。

若权限检查或一次性容器检查失败,不要执行 `up -d portal`,线上旧容器继续运行。先检查本次新增文件的
属主和 mode:

```bash
find prisma/migrations -maxdepth 2 -printf '%M %u:%g %p\n' | tail -30
docker compose -f docker-compose.prod.yml run --rm --no-deps -T --interactive=false --entrypoint sh portal -c \
  'id; find prisma/migrations -maxdepth 2 -exec ls -ld {} \; | tail -30'
```

若启动时报 Prisma `P3015` / `migration.sql missing`,先按上述命令确认“宿主机文件存在但 runner
不可读”。在确认数据库 `_prisma_migrations` 状态前,禁止运行 `prisma migrate resolve`、禁止补造空 SQL、
禁止删除 migration 目录、禁止恢复数据库。权限修好后重新 build 并再次运行只读检查;只有检查通过才切换
容器。若文件在 Git 中确实不存在,停止发布并回到本地修复提交历史,不要直接在 VPS 手工补文件。

构建阶段若出现 `Can't resolve '@vercel/turbopack-next/internal/font/google/font'` 或 `next/font/google
queries have exactly one entry`,是构建时拉取 Google Fonts 的网络瞬断,与代码无关;线上旧容器不受影响,
确认 `curl -I https://fonts.googleapis.com` 可达后直接重跑同一条 build 命令即可(2026-09-24 实测重跑成功)。

### 4.4.1 用回滚标签快速回退

回退到上一版时不需要重新构建,直接让 `portal` 服务改用回滚标签的镜像。必须先确认本次发布没有新增
migration(Prisma migration 是 forward-only,旧代码未必兼容新 schema),并且定价发布协调器没有活动任务
或 `in_flight` 写入:

```bash
cd /opt/silkroadai-portal
docker images --format '{{.Tag}} {{.ID}} {{.CreatedSince}}' silkroadai-portal-portal | sort -r | head
docker tag silkroadai-portal-portal:rollback-<TS> silkroadai-portal-portal:latest
docker compose -f docker-compose.prod.yml up -d --no-build portal
curl -I --max-time 5 http://127.0.0.1:3002/login
```

回退后服务器 Git 仍在新的 `prod` SHA 上,只是运行镜像是旧的;事后必须按 8.2 在 `dev` 做 revert 并正式
发布,让 Git 与运行镜像重新一致。若回滚标签缺失,只能按 8.2 用已知正常 SHA 重新构建(约 5 分钟)。

### 4.5 2026-08-04 已部署批次的附加检查

该批次不只是文案,还包含上游同步、两个 forward migration 和生产入口加固。2026-08-04 已按本节完成
生产部署和验证;后续重建或排障仍按以下契约检查:

1. `20260803100000_update_platform_support_wechat`:把平台租户微信更新为 `LLmRoute`。
2. `20260730080000_add_aksk_secret_key_hash`:给 enterprise AK/SK 增加 nullable hash 和唯一索引。
3. Docker/Compose 生产构建变量与可选服务 profile 隔离。
4. `api.llmroute.club` API-only Nginx virtual host,以及 `/v1` 浏览器 CORS 预检透传。
5. `main@eb1258a` 的 7 个上游提交:视频渠道 480p/4-15 秒支持与内部 image-adapter。

发布前在服务器 `.env` 确认:

```env
NEXT_PUBLIC_SUPPORT_WECHAT="LLmRoute"
NEWAPI_CUSTOMER_BASE_URL="http://127.0.0.1:3002"
```

第二项是运行时变量。缺失时 Portal Chat/Image 会回退到公网 `api.llmroute.club`,形成不必要的
Cloudflare 回源环路;不能因为模板已更新就假设服务器现有 `.env` 自动拥有它。

发布后检查 migration:

```bash
docker logs --since=10m silkroadai-portal 2>&1 | grep -Ei 'migration|error|failed'
docker exec -i silkroadai-portal-db psql -U portal silkroadai_portal_prod \
  -c 'SELECT migration_name, finished_at FROM "_prisma_migrations" ORDER BY finished_at DESC NULLS LAST LIMIT 5;'
```

如果 `enterprise_ak_sk` 已有存量行,新列不会自动回填。先统计:

```bash
docker exec -i silkroadai-portal-db psql -U portal silkroadai_portal_prod \
  -c 'SELECT count(*) AS missing_hash FROM enterprise_ak_sk WHERE secret_key_hash IS NULL;'
```

结果大于 0 时必须按 `scripts/backfill-aksk-secret-hash.ts` 先 dry-run 再 `--apply`;生产 runner 镜像不包含该脚本,
需要单独准备包含完整仓库依赖的一次性执行环境。未确认执行环境和数据库备份前不要临时拼命令。

---

## 5. 发布后验证

### 5.1 容器、migration 和内部链路

```bash
cd /opt/silkroadai-portal
docker compose -f docker-compose.prod.yml ps portal portal-postgres
docker logs --since=10m silkroadai-portal 2>&1 | tail -100

curl -I --max-time 5 http://127.0.0.1:3002/login
curl -sS --max-time 5 http://172.17.0.1:3000/api/setup

docker exec silkroadai-portal node -e \
  "fetch('http://host.docker.internal:3000/api/setup').then(async r=>console.log(r.status,await r.text())).catch(e=>{console.error(e);process.exit(1)})"
```

期望:

- Portal 和 DB 为 `Up`,DB 为 `healthy`。
- `/login` 为 200。
- new-api `/api/setup` 返回 JSON。
- 容器到 new-api 返回 `200`。
- 日志没有 migration error、`ECONNREFUSED` 或持续 5xx。

### 5.2 Nginx、Cloudflare 和公网

```bash
nginx -t
systemctl is-active nginx
ss -lntp | grep -E ':(80|443)\b'

curl -k -I --resolve llmroute.club:443:127.0.0.1 https://llmroute.club/login
curl -I --max-time 15 https://llmroute.club/login
curl -I --max-time 15 https://www.llmroute.club/login
```

期望三次都是 200。第一条绕过 Cloudflare 验证源站;后两条验证 Cloudflare 全链路。

### 5.3 API 子域

先测源站 virtual host,再测 Cloudflare 公网。下列假 Key 只用于确认请求经过 Portal/new-api 并得到
401,不产生模型调用或费用:

```bash
# 源站:绕过 Cloudflare,但保留 api.llmroute.club 的 SNI/Host。
curl -k -sS -o /dev/null -w 'origin /v1/models -> %{http_code}\n' \
  --resolve api.llmroute.club:443:127.0.0.1 \
  -H 'Authorization: Bearer invalid-smoke-token' \
  https://api.llmroute.club/v1/models
curl -k -sS -o /dev/null -w 'origin /login -> %{http_code}\n' \
  --resolve api.llmroute.club:443:127.0.0.1 \
  https://api.llmroute.club/login

# 公网:经过 Cloudflare。
curl -sS -o /dev/null -w 'public /v1/models -> %{http_code}\n' \
  -H 'Authorization: Bearer invalid-smoke-token' \
  https://api.llmroute.club/v1/models
curl -sS -o /dev/null -w 'public /login -> %{http_code}\n' \
  https://api.llmroute.club/login
```

期望 `/v1/models` 为 401,`/login` 为 404。再分别检查 OpenAI 与 Gemini 原生接口的浏览器预检:

```bash
curl -sS -o /dev/null -D - -X OPTIONS \
  https://api.llmroute.club/v1/chat/completions \
  -H 'Origin: https://example.com' \
  -H 'Access-Control-Request-Method: POST' \
  -H 'Access-Control-Request-Headers: authorization,content-type' \
  | grep -iE 'HTTP/|access-control-allow-(origin|headers|methods)'

curl -sS -o /dev/null -D - -X OPTIONS \
  'https://api.llmroute.club/v1beta/models/gemini-test:generateContent' \
  -H 'Origin: https://example.com' \
  -H 'Access-Control-Request-Method: POST' \
  -H 'Access-Control-Request-Headers: x-goog-api-key,content-type' \
  | grep -iE 'HTTP/|access-control-allow-(origin|headers|methods)'
```

两次都应为 204,且包含 `Access-Control-Allow-Origin`、`Headers` 和 `Methods`。最后用一个低余额测试账号的
真实 API Key 完成 `/v1/models` 200 和一次低成本流式文本调用;Key 用 `read -rsp` 放入临时 shell 变量,
完成后立刻 `unset`,不要写进文档、命令历史或截图。

### 5.4 OAuth

```bash
curl -sS -o /dev/null -D - https://llmroute.club/api/auth/oauth/google/start \
  | grep -iE 'HTTP/|^location:'
curl -sS -o /dev/null -D - https://llmroute.club/api/auth/oauth/github/start \
  | grep -iE 'HTTP/|^location:'
```

分别应 302 到 `accounts.google.com` 和 `github.com/login/oauth/authorize`。最终验收必须在浏览器各登录一次,
不能只看 302。失败时看:

```bash
docker logs --since=10m silkroadai-portal 2>&1 \
  | grep -Ei 'oauth|account-link|provision|new-api|error|failed'
```

当前兼容策略:new-api rc.22+ 登录响应体中的 `data.access_token` 是短效会话 JWT，只用于认证
`GET /api/user/token`，换取 opaque 持久访问令牌后才写入 Portal `User.newapi_access_token`；旧版本用
`session=` cookie 调同一端点。不要把 `new_api_refresh` cookie 当 legacy session，也不要把登录 JWT
直接持久化。发布后应确认已开户用户的 token 非空、不是 JWT，并通过只读 `/api/user/self` 鉴权且返回
匹配的 new-api 用户 ID；不要按固定长度判断（已实际验证 28 字符持久 token 可用）。误存短效 JWT 会在
过期后导致创建/删除 Key 返回 new-api 401、Portal 502。不要为了验收调用会旋转令牌的 `/api/user/token`。
事故与合并审计规则见 `AGENTS.md`。

### 5.5 支付

自动检查只能确认路由,最终必须做一笔受控的小额真实支付:

1. 登录测试账号,打开 `/pay`。
2. 发起最低金额充值并选择支付宝。
3. 确认进入 zpayz 收银台。
4. 支付后确认回到 `/pay/result`。
5. 在 `/balance` 确认余额增加且只入账一次。
6. 检查日志没有签名失败或重复入账异常。

```bash
docker logs --since=15m silkroadai-portal 2>&1 \
  | grep -Ei 'easy-pay|notify|recharge|signature|error|failed'
```

zpayz 异步通知要求返回纯字符串 `success`;重复通知由订单状态机幂等处理。商户 PID 可以记录在模板,
商户 KEY 只能保存在服务器 `.env`。

---

## 6. Nginx 与 Cloudflare

### 6.1 当前证书

```text
/etc/nginx/ssl/llmroute.club.pem
/etc/nginx/ssl/llmroute.club.key
```

私钥权限必须是 600。证书为 Cloudflare Origin CA,有效期核验:

```bash
chmod 600 /etc/nginx/ssl/llmroute.club.key
openssl x509 -in /etc/nginx/ssl/llmroute.club.pem -noout -subject -issuer -dates -ext subjectAltName
```

证书和私钥匹配核验:

```bash
openssl x509 -in /etc/nginx/ssl/llmroute.club.pem -pubkey -noout \
  | openssl pkey -pubin -outform der | sha256sum
openssl pkey -in /etc/nginx/ssl/llmroute.club.key -pubout \
  | openssl pkey -pubin -outform der | sha256sum
```

两个 SHA-256 必须完全相同。

### 6.2 当前主站路由要求

Nginx 中 `llmroute.club` 和 `www.llmroute.club` 应共同反代 `127.0.0.1:3002`,HTTP 跳 HTTPS。
仓库基线模板为 `deploy/nginx/llmroute-web.conf`,生产安装路径为
`/etc/nginx/conf.d/llmroute.club.conf`。不要只在服务器临时 `sed`,否则配置无法审计和复现。
该模板的 access log 只记录 `$uri`,不记录 query string,避免 OAuth code、密码重置/邮箱验证 token 和
支付回调签名落盘。
`/image-adapter/*` 是 new-api 的内部上游适配器,主站 Nginx 必须对公网返回 404。若未来启用该图片渠道,
new-api 的 Base URL 使用共享 Docker 网络地址
`http://silkroadai-portal:3002/image-adapter/ominiapi`,上游 Key 只保存在 new-api 渠道配置中;禁止改成
`https://llmroute.club/image-adapter/ominiapi` 绕公网调用。
修改任何 Nginx 配置后统一执行:

```bash
nginx -t && systemctl reload nginx
```

Cloudflare DNS 的 apex 与 `www` 记录都应指向 `82.29.71.122` 并开启橙云。SSL/TLS 模式保持
**Full (strict)**,不要使用 Flexible。

### 6.3 `api.llmroute.club` 独立入口

固定路径策略如下:

- 只有 `/v1/*` 和 `/v1beta/*` 对公网开放,两者都先进入 Portal `127.0.0.1:3002`。
- 不把 API 子域直接代理到 new-api,否则会绕过 Gemini 图片转换、请求体守门、Codex clamp、日志捕获和
  SSE keep-alive 等 Portal 能力。
- `/login`、`/admin/*`、支付和 Portal 内部 API 在该 hostname 一律返回 JSON 404;管理入口只留主站域名。
- SSE 关闭 Nginx buffering/cache,长图片/视频请求 read/send timeout 为 650 秒,请求体上限 100 MB。
- access log 只记录 `$uri`,不记录 `$request_uri`,避免 Gemini SDK 的 `?key=` 凭据写入源站日志。

仓库模板是 `deploy/nginx/llmroute-api.conf`。部署前先查看并备份真实配置:

```bash
nginx -T 2>&1 | grep -E '^# configuration file|listen|server_name|ssl_certificate|proxy_pass|client_max_body_size|proxy_(buffering|request_buffering|read_timeout)'
ls -la /etc/nginx/conf.d/

umask 077
tar -C /etc/nginx -czf "/root/nginx-config-before-api.$(date +%Y%m%d-%H%M%S).tar.gz" nginx.conf conf.d
```

拉取本次 `prod` 后同时安装主站与 API 模板。主站模板只在现有配置基础上显式补入 `www`;API 模板
新增独立 virtual host。`nginx -t` 失败时不得 reload:

```bash
install -o root -g root -m 0644 \
  /opt/silkroadai-portal/deploy/nginx/llmroute-web.conf \
  /etc/nginx/conf.d/llmroute.club.conf
install -o root -g root -m 0644 \
  /opt/silkroadai-portal/deploy/nginx/llmroute-api.conf \
  /etc/nginx/conf.d/llmroute-api.conf
nginx -t && systemctl reload nginx
```

Cloudflare 必须同时满足:

1. 显式 `A api -> 82.29.71.122`,代理状态为橙云;不要只依赖 wildcard 记录。
2. SSL/TLS 模式为 **Full (strict)**。
3. Cache Rules 中 `Hostname equals api.llmroute.club` 设置 **Bypass cache**;API/SSE 不允许缓存。
4. Configuration Rules 中启用 `LLmRoute API - Disable Browser Integrity Check`，只对以下表达式
   设置 `bic: false`，全局 BIC 保持开启：

```text
(http.host eq "api.llmroute.club" and (starts_with(http.request.uri.path, "/v1/") or starts_with(http.request.uri.path, "/v1beta/")))
```

此规则 ID 为 `d3b136c9eda64005baf0a43257a793b1`，2026-09-09 启用后默认 urllib 的真实 Key 模型列表
200、假 Key 401，两类 CORS 204，主站原 BIC 与 API 路径隔离仍通过。需要回滚时只 Disable 此规则。
2026-09-09 21:23 另启用 Custom Rule `LLmRoute API - Skip SBFM bot checks`（Rule ID
`5804effbe8504e7386f78e6715d4bccb`，ruleset `c66d62c0ae7541caa27328d72174649c`），匹配表达式与上方
BIC 例外相同；只设置 `action_parameters.phases=["http_request_sbfm"]`，匹配日志开启。它跳过该 API
范围内整个 SBFM 阶段的现有及未来规则，未跳过 WAF managed/custom、rate limiting、HTTP DDoS；Free
Bot Fight Mode 不能据此跳过，主站规则保持不变。不要把它改为跳过全部 managed rules 或剩余 custom rules。
官方 OpenAI Python SDK 3.10.0 默认头真实 Key 模型列表已为 200；OpenAI/Anthropic SDK 的无效 Key
GET/POST 均为 401 JSON，六项 SDK 检查及 16 项边界检查通过，未发起收费推理。需要回滚时只 Disable
此条 SDK 规则，不删除整个 custom ruleset；临时管理 Token 已主动撤销。完整证据与验收边界见
[Cloudflare 客户端兼容记录](../docs/CLOUDFLARE-API-COMPAT-2026-09-09.md)。

安装后按第 5.3 节验收,并确认日志没有查询参数泄漏。这里使用的是无效测试串,不是真 Key:

```bash
curl -k -sS -o /dev/null \
  --resolve api.llmroute.club:443:127.0.0.1 \
  'https://api.llmroute.club/v1beta/models/test:generateContent?key=redaction-smoke'
if grep -F 'redaction-smoke' /var/log/nginx/api.llmroute.club.access.log; then
  echo 'FAIL: API query string leaked into access log' >&2
  exit 1
fi
```

紧急回滚该 virtual host:

```bash
mv /etc/nginx/conf.d/llmroute-api.conf /etc/nginx/conf.d/llmroute-api.conf.disabled
nginx -t && systemctl reload nginx
```

这只回滚 Nginx 路由,会让该域名重新落入 default virtual host;随后仍需按 Git 发布流程修复,不能把
default host 当作长期 API 入口。

---

## 7. new-api 运维

### 7.1 当前容器重建基线

new-api 当前不是 Compose 管理,重建命令基线如下。执行前必须确认 `/root/new-api.env` 和
`/opt/new-api/data` 已备份,并且 `new-api_new-api-net` 存在:

```bash
docker run -d \
  --name new-api \
  --restart always \
  --network new-api_new-api-net \
  --env-file /root/new-api.env \
  -p 172.17.0.1:3000:3000 \
  -v /opt/new-api/data:/data \
  calciumion/new-api:latest
```

不要在旧容器仍占名时直接运行。任何 stop/rm/rename 前先 `docker inspect` 保存当前镜像、环境文件、
端口、网络和挂载信息。日常只需:

```bash
docker restart new-api
docker logs --since=10m new-api
```

### 7.2 连通性检查

```bash
docker ps --filter name=new-api
docker inspect new-api --format '{{range $name, $conf := .NetworkSettings.Networks}}{{$name}}{{"\n"}}{{end}}'
ss -lntp | grep -E ':3000\b'
curl -sS http://172.17.0.1:3000/api/setup
```

端口应显示 `172.17.0.1:3000->3000/tcp`,网络应包含 `new-api_new-api-net`。

### 7.3 从 Mac 访问 new-api 后台

new-api 不对公网开放。以下命令必须在 operator Mac 执行,不能在 VPS shell 中执行:

已配置 `~/.ssh/config` 中的 `llmroute-newapi` 别名时,首选:

```bash
if ssh -O check llmroute-newapi >/dev/null 2>&1; then
  ssh -O forward -o ExitOnForwardFailure=yes llmroute-newapi
else
  ssh -fN -o ExitOnForwardFailure=yes llmroute-newapi
fi
curl --noproxy '*' --fail --max-time 10 http://127.0.0.1:3000/api/status
```

返回 `success:true` 后浏览器打开 `http://127.0.0.1:3000/`。当前别名使用 control socket
`~/.ssh/llmroute-newapi.sock`,并转发 `127.0.0.1:3000 -> 172.17.0.1:3000`;查看和停止使用:

```bash
ssh -O check llmroute-newapi
ssh -O exit llmroute-newapi
```

需要重连时（例如服务器刚切换拥塞控制算法），先正常关闭，再重新建立：

```bash
ssh -O exit llmroute-newapi
lsof -nP -iTCP:3000 -sTCP:LISTEN
# 确认上面的命令没有监听输出后，再执行：
ssh -fN llmroute-newapi
ssh -O check llmroute-newapi
```

日常使用前面的分支命令：master 已存在时补齐配置里的端口转发，不存在时新建连接；
转发失败直接显示错误，不将端口占用等错误误当成“没有master”再尝试新建。
仍须以 `/api/status` 的 HTTP 200 和 `success:true` 确认 new-api 实际可达。
`ssh -O check` 只检查 master 进程，不能证明 3000 正在监听或远端服务可达。
2026-09-14 实测 master 存在但没有任何本地监听，旧 check-or-connect 因此跳过建立隧道；
`ssh -O forward llmroute-newapi` 在同一 master 上补齐转发后恢复，再次执行也成功，未中断已有连接。

运维命令若使用 `ClearAllForwardings=yes`，须同时设置 `ControlPath=none` 与 `ControlMaster=no`，
使用独立连接，避免在隧道的 control socket 上留下没有转发的后台 master。
本次修复未改 Mac SSH 配置或重启线上服务。`exit` 会关闭该 master 及其复用会话。
不要通过删除 control socket 来重连：删 socket 不会关闭旧 SSH 进程。
`ssh -O check` 失败也不能单独证明进程不存在；还须核对 `lsof` 的监听 PID 和进程命令，
确认无旧 master 且 socket 确实陈旧后，才将旧 socket 改名留档。

未配置该别名或本地 3000 端口冲突时,可使用独立的 18082 端口:

```bash
ssh -F /dev/null -4 -f -N -T \
  -i "$HOME/.ssh/llmroute_ed25519" \
  -o IdentitiesOnly=yes \
  -o IPQoS=none \
  -o TCPKeepAlive=yes \
  -o ServerAliveInterval=15 \
  -o ServerAliveCountMax=4 \
  -o ExitOnForwardFailure=yes \
  -o ControlMaster=no \
  -o ControlPath=none \
  -L 127.0.0.1:18082:172.17.0.1:3000 \
  root@82.29.71.122

curl --max-time 10 http://127.0.0.1:18082/api/setup
```

返回 `success:true` 后浏览器打开 `http://127.0.0.1:18082/`。`-f` 会让隧道在认证完成后进入后台;
查看时先核对监听 PID 和完整进程命令：

```bash
lsof -nP -iTCP:18082 -sTCP:LISTEN
```

再用 `ps -p <PID> -o pid,ppid,command` 确认它就是刚建立的 SSH 隧道，才用 `kill <PID>` 停止。
不要按端口直接批量 kill，也不要用宽泛 `pkill`。`-F /dev/null` 排除别名附带的转发，
`ControlPath=none` 避免复用已有 master；仅 `ControlMaster=no` 不保证独立连接。

如果已有旧隧道,先停止旧监听再重建。服务器切换拥塞控制算法后,已经建立的 SSH 连接不会自动采用
新算法,也必须重建。

### 7.4 new-api 技术计费单位迁移(500000 quota = ¥1)

本节是一次性生产迁移。只有 operator 明确安排维护窗口后才能执行。代码发布与参数切换必须分开:
先发布兼容代码并保持生产旧值不变,确认正常后再进入停写窗口。不得只修改
`USD_TO_CNY_RATE`,否则旧余额会缩小 7.2 倍;也不得只放大余额而不重同步 Portal 目录模型,否则这些
模型会少扣 7.2 倍。

迁移后的固定不变量:

- new-api `QuotaPerUnit=500000` 保持不变。
- Portal `NEWAPI_QUOTA_PER_USD=500000`、`USD_TO_CNY_RATE=1`、
  `REAL_USD_TO_CNY_RATE=7.2`。
- 客户充值 ¥1 增加 500000 raw quota;500000 raw quota 在 Portal 显示为 ¥1。
- 真实美元展示为 `人民币 ÷ REAL_USD_TO_CNY_RATE`,真实汇率不参与人民币扣费。
- 公共 `GroupRatio`、内部用户组、`GroupGroupRatio` 专属规则和 Token.group 都不因本迁移改变。

#### 7.4.1 发布兼容代码,暂不切参数

按第 4 节发布包含计费单位兼容修复的代码,但服务器 `.env` 暂时继续保持原值
`USD_TO_CNY_RATE=7.2`,且暂不新增 `REAL_USD_TO_CNY_RATE`。新代码在该组合下行为中性。确认 Portal、
充值、余额和 new-api 链路正常后,再预约低流量维护窗口。

#### 7.4.2 停写与三份快照

维护开始后先停止 Portal,阻断 `/v1` 调用、充值回调和后台写操作:

```bash
cd /opt/silkroadai-portal
docker compose -f docker-compose.prod.yml stop portal
curl -sS -o /dev/null -w '%{http_code}\n' https://api.llmroute.club/v1/models
```

第二条应不再返回正常业务响应。随后依次完成并校验三份快照:

1. 按 4.3 节运行 `scripts/backup-db.sh`,并执行 `gzip -t`。
2. 复制当前 `.env` 为 0600 的带时间戳备份。
3. 对 `new-api-mysql` 做 `--single-transaction` 全库备份,并另存以下可读快照:
   Portal 所有已映射的 `newapi_user_id`;每个用户的 `id/quota/used_quota/group`;完整
   option 响应(至少核对 `ModelRatio`、`CompletionRatio`、`GroupRatio`、`GroupGroupRatio`、
   `UserUsableGroups`);Portal 当前所有已启用的目录模型、人民币价格、档次和 upstream model 映射。

new-api MySQL 和 `/opt/new-api/data` 统一按 8.3 节的 `set -euo pipefail` 流程备份。不得简化为
`mysqldump | gzip > file` 而不开 `pipefail`,否则 dump 失败可能生成伪成功压缩包。

不要把 access token、`.env` 内容或完整用户 JSON 打到终端/聊天。快照目录和文件均用 0600/0700。

#### 7.4.3 计算并覆盖余额

对每个 Portal 映射用户使用停写后快照的 `old_quota` 和 `old_used_quota` 计算:

```text
target_quota = round((old_quota + old_used_quota) × 7.2 - old_used_quota)
             = round(old_quota × 7.2 + old_used_quota × 6.2)
```

含义是先按旧口径还原累计人民币额度,再按新口径扣回历史真实消费。通过 new-api
`POST /api/user/manage` 的 `add_quota + override` 写入 `target_quota`;禁止直接 `PUT /api/user/` 改 quota,
以免触发用户字段覆盖。每写一个用户立即 `GET /api/user/{id}` 回读并核对 quota、used_quota 和 group。
请求体固定为
`{"id": <new-api user id>, "action": "add_quota", "mode": "override", "value": <target_quota>}`。

2026-08-24 只读审计时的参考值如下;执行前必须重新快照,若值变化则按新快照重算,不能照抄旧数字:

| new-api user | 审计时旧余额 quota | 审计时 used_quota |             参考 target_quota |
| ------------ | -----------------: | ----------------: | ----------------------------: |
| 6            |      6,848,292,597 |       235,874,067 |                50,770,125,914 |
| 12           |            347,222 |                 0 | 2,500,000（按原 ¥5 调整记录） |
| 13           |            347,222 |                 0 | 2,500,000（按原 ¥5 调整记录） |

其余审计时余额和用量均为 0。普通客户历史调用为 0;唯一历史调用用户是 superadmin 测试账号,因此该次
迁移不涉及普通客户退款。若执行前出现普通客户调用,必须重新做逐用户账务审计后再继续。

#### 7.4.4 切换环境变量与模型倍率

编辑生产 `.env` 为:

```env
NEWAPI_QUOTA_PER_USD=500000
USD_TO_CNY_RATE=1
REAL_USD_TO_CNY_RATE=7.2
```

然后在 new-api 后台“系统设置 → 计费与支付 → 分组定价 → 切换到 JSON”中,基于快照完整合并配置。
只更新 Portal 目录管理的模型,不能覆盖其他模型。2026-08-24 审计时只有以下两项:

- `claude-fable-5`: `ModelRatio` 从 `0.694444` 改为 `5`,其 `CompletionRatio` 保持 `5`。
- `kimi-k3`: `ModelRatio` 从 `1.388889` 改为 `10`,其 `CompletionRatio` 保持 `5`。
- 非 Portal 目录管理模型保持原倍率;`GroupRatio`、`GroupGroupRatio`、`UserUsableGroups` 不改。

执行前必须与 7.4.2 的最新 Portal 目录快照再次比对。若已启用目录模型不再只有这两项,停止迁移并为
每个新增模型补齐新口径倍率清单;chat 模型按
`ModelRatio = 输入人民币价 ÷ (2 × 该档 GroupRatio)` 计算(`2` 是新口径下 100 万 quota 的人民币值),
按张模型也必须按新口径重算 `ModelPrice`。不能把“审计时只有两项”当作永久假设。

保存后重新读取完整 option,逐项确认其他模型和其他用户的规则仍在。再清空 Portal quota 缓存:

```bash
docker exec -i silkroadai-portal-db psql -U portal silkroadai_portal_prod \
  -c 'UPDATE users SET newapi_quota_cache = NULL, newapi_used_quota_cache = NULL, newapi_cached_at = NULL WHERE newapi_user_id IS NOT NULL;'
```

最后按 4.4 节重建/启动 Portal。不得启用 `api-replicas` 或 `seedance` profile。

#### 7.4.5 验收与停止条件

启动后先用 superadmin 测试账号做受控小额验证,普通客户继续保持无流量:

1. Portal 余额人民币值与迁移清单一致;美元值约为人民币值除以 7.2。
2. 管理员充值 ¥1 时,new-api 用户余额精确增加 500000 quota。
3. `claude-fable-5` 与 `kimi-k3` 的同类请求扣费符合目录人民币价格;new-api 日志 raw quota 与 Portal
   人民币显示满足 `Portal ¥ = raw quota ÷ 500000`。
4. 未由 Portal 目录管理的模型做一笔样例,确认原 ModelRatio 未被覆盖。
5. 专属倍率用户的 `User.group`、旧 Key 和新 Key 的 Token.group 均未改变,且 `GroupGroupRatio` 仍命中。

任一余额、倍率、专属规则或模型价格不符,立即停止测试流量并回滚,不能边开放客户流量边修数。

#### 7.4.6 回滚

回滚也必须先停止 Portal。按快照逐个用 `/api/user/manage` `override` 恢复原 quota,完整恢复迁移前
`ModelRatio` / `CompletionRatio` option,恢复 `.env` 备份(`USD_TO_CNY_RATE=7.2`,删除新增的
`REAL_USD_TO_CNY_RATE`),再次清空 Portal quota 缓存并重启。逐个回读验证后才能恢复流量。

只有 API 级回滚失败或数据已无法与快照一致时,才考虑恢复 new-api MySQL 全库备份;全库恢复会覆盖维护
窗口后的所有 new-api 状态,必须保持停写并先再次备份故障现场。代码本身兼容旧参数,通常无需回滚代码。

### 7.5 官方 new-api 图片分辨率按张计费

生产继续使用官方 `calciumion/new-api:v1.0.0-rc.23`,不替换镜像、不部署修改版。官方 rc.23 的
`ModelPrice` 会在渠道 `model_mapping` 之前按客户原始模型名查价,因此用三个独立 SKU
即可实现分辨率固定价:

```json
{
    "gpt-image-2-1k": 5,
    "gpt-image-2-2k": 7.5,
    "gpt-image-2-4k": 10
}
```

固定不变量:

- new-api `QuotaPerUnit=500000`;Portal `NEWAPI_QUOTA_PER_USD=500000`、`USD_TO_CNY_RATE=1`。
- 图片渠道的有效 `GroupRatio=0.2`,`GroupGroupRatio` 不得对该使用组覆盖为其他值。
- raw quota = `ModelPrice × 500000 × 0.2 × n`;客户人民币 = `raw quota / 500000`。
- 上游充值 `1:10` 只是采购成本换算,不写入 `GroupRatio`、`ModelPrice` 或渠道倍率。

所以单张预期扣费为:

| 客户模型         | Portal 固定尺寸 | ModelPrice | raw quota | 人民币 |
| ---------------- | --------------- | ---------: | --------: | -----: |
| `gpt-image-2-1k` | `1024x1024`     |          5 |   500,000 |  ¥1.00 |
| `gpt-image-2-2k` | `2048x2048`     |        7.5 |   750,000 |  ¥1.50 |
| `gpt-image-2-4k` | `3840x2160`     |         10 | 1,000,000 |  ¥2.00 |

最终状态只从图片渠道移除 canonical `gpt-image-2`,新增上述三个别名;渠道中
`gpt-image-2-cf` / Grok / Nano Banana 等非目标模型原样保留。`model_mapping` 完整合并:

```json
{
    "gpt-image-2-1k": "gpt-image-2",
    "gpt-image-2-2k": "gpt-image-2",
    "gpt-image-2-4k": "gpt-image-2"
}
```

#### 7.5.1 上线前置与维护窗口

2026-08-25 只读审计确认目标是唯一含 `gpt-image-2` 的渠道 `#12`:
`type=1,status=1,group=图片模型,model_mapping={}`。执行前必须重新回读,不能把这份历史快照当成写入依据。

硬性前置:

1. `dev` 已通过完整验证并 fast-forward 到 `prod`,VPS 可在维护前预先 `build portal`,
   但 operator 没有明确安排时不得替换运行容器。
2. 确认不存在正在使用的“图片模型”客户 Key;若存在,先停用该流量。
   `channel_groups.newapi_channel_ids` 为空只代表 Portal 尚未登记渠道,不能证明 new-api 没有已发 Key。
3. 维护期间禁止另一个管理员编辑 new-api 价目或渠道。API 没有跨 option/channel 事务和 CAS。
4. 保留当前 `prod` SHA、Portal 镜像 ID、new-api 镜像 digest 和脚本快照路径。

维护开始后在 VPS 先停 Portal,再停止共用 MySQL 和 `/opt/new-api/data` 的旧容器;不删除它:

```bash
set -euo pipefail
cd /opt/silkroadai-portal
docker compose -f docker-compose.prod.yml stop portal

if docker container inspect new-api-old-port >/dev/null 2>&1; then
  docker update --restart=no new-api-old-port >/dev/null
  docker stop new-api-old-port
  test "$(docker inspect new-api-old-port --format '{{.State.Running}}')" = 'false'
fi
```

然后先按 4.3 节运行并验证 Portal PostgreSQL 备份,再按 8.3 节备份 new-api MySQL、
`/opt/new-api/data`、容器 inspect 和两份环境文件。

#### 7.5.2 Pricing 持久化验证 + Prepare 兼容配置

在 operator Mac 按 7.3 节确认隧道后,用 new-api root 用户的 **32 字符持久 access token**
执行。不能使用登录响应中数百字符的短效 JWT。下列读取方式不会把 token 写入 shell history:

```bash
cd /Users/elon/work/code/ai/silkroadai
printf 'new-api root persistent access token: '
IFS= read -r -s NEWAPI_ADMIN_TOKEN
printf '\n'
export NEWAPI_ADMIN_TOKEN
export NEWAPI_BASE_URL='http://127.0.0.1:3000'
export NEWAPI_IMAGE_CHANNEL_IDS='12'
export NEWAPI_IMAGE_GROUP='图片模型'

# 第一阶段只改 ModelPrice,不动渠道。
node scripts/configure-gpt-image-resolution-skus.mjs --phase=pricing
node scripts/configure-gpt-image-resolution-skus.mjs --phase=pricing --apply
```

记录脚本输出的 `gpt-image-pricing-....json` 绝对路径。官方 rc.23 的 option PUT 回读来自内存,
不足以证明 MySQL 已持久化。先在 Mac 对快照中的完整 `ModelPrice` 求 SHA-256:

```bash
PRICING_SNAPSHOT='/absolute/path/to/gpt-image-pricing-....json'
jq -jr '.after.model_price' "$PRICING_SNAPSHOT" | shasum -a 256 | awk '{print $1}'
```

再在 VPS 直接查 MySQL 中的值,两个哈希必须完全一致;API 输出不能代替这一步:

```bash
docker exec new-api-mysql sh -lc \
  'exec mysql -N -B -uroot -p"$MYSQL_ROOT_PASSWORD" "$MYSQL_DATABASE" -e \
  "SELECT LOWER(SHA2(CAST(value AS BINARY),256)) FROM options \
  WHERE \`key\`=0x4d6f64656c5072696365;"'
```

哈希一致后,在 VPS 受控重启当前正式容器。脚本只支持官方
`v1.0.0-rc.23`,版本不符会直接拒绝继续:

```bash
set -euo pipefail
docker restart new-api
for _ in $(seq 1 30); do
  if curl -fsS --max-time 2 http://172.17.0.1:3000/api/setup >/dev/null; then break; fi
  sleep 1
done
curl -fsS --max-time 5 http://172.17.0.1:3000/api/setup >/dev/null

# 等待至少一轮 60s Option 数据库同步;脚本会强制要求进程已运行 >=65s。
sleep 65
```

回到 Mac,先用 pricing 快照做只读重启验证。脚本会要求 new-api `start_time` 已变大,
且重启并经过同步周期后的完整 `ModelPrice` 与渠道状态仍等于快照 after-state:

```bash
node scripts/configure-gpt-image-resolution-skus.mjs \
  --phase=verify --snapshot="$PRICING_SNAPSHOT" --expect=after

# 第二阶段只加别名/映射,仍保留 canonical。
node scripts/configure-gpt-image-resolution-skus.mjs \
  --phase=prepare --pricing-snapshot="$PRICING_SNAPSHOT"
node scripts/configure-gpt-image-resolution-skus.mjs \
  --phase=prepare --pricing-snapshot="$PRICING_SNAPSHOT" --apply
```

`pricing` 只完整合并 `ModelPrice` 的三条价格;`prepare` 只给 `#12` 追加别名/映射。脚本还会拒绝
`tiered_expr` / 启用中的定时折扣 / `ImageResolutionPrice` 这三类会绕过或改写固定价的配置,
也不会覆盖别名已有的非 canonical 映射。此时仍保留
canonical,且 Portal 仍保持停机,所以不存在别名经旧 Portal 改回 canonical 后按旧低价扣费的窗口。
脚本在项目根目录下的 `.newapi-config-snapshots/` 生成 0600 快照,任一写入/回读失败会按逆序恢复本轮已写内容。
成功 cutover 后重跑 `prepare` 会拒绝把 canonical 重新加回;需要回退只能用明确的 `restore`。

#### 7.5.3 部署 Portal、导入目录并真实验账

在 VPS 用第 4 节已通过验证的 `prod` 镜像重建/启动 Portal,核对容器运行 SHA、
restart count、PostgreSQL migration 和 Portal -> new-api 链路。图片组仍不对真实客户开放。

登录 Portal 超级管理后:

1. 在 `/admin/channel-groups` 编辑 `newapi_group=图片模型` 的档次,把渠道 ID 登记为 `12`;
   这个字段用于目录导入/审计,真实路由仍由 new-api `token.group -> channel.group` 完成。
2. 在 `/admin/models` 只选 `#12` 先 dry-run 导入,再确认导入。三个 slug 必须保持为
   `gpt-image-2-1k/2k/4k`,其 `upstream_map[*].upstream_model` 也必须是同一别名。
3. 分别填 `per_image_cny=1/1.5/2`。保存后回读 new-api `ModelPrice`,仍必须是 `5/7.5/10`;
   不得把三个目录 slug 合并成 canonical。

用受控测试账号的“图片模型” Key 分别调用 1K、2K、4K 单张,再选一档调用 `n=2`。
每次记录前后 raw quota、日志 `model_price`、原始模型名、尺寸和 `n`。差值必须为:

| 请求         |  raw quota 差值 |
| ------------ | --------------: |
| 1K,`n=1`     |         500,000 |
| 2K,`n=1`     |         750,000 |
| 4K,`n=1`     |       1,000,000 |
| 任一档,`n=2` | 该档单张的 2 倍 |

同时验证:冲突 `size` / `aspect_ratio` / `stream:true` 均在转发前返回 400 且 quota 不变;
上游尺寸错误不降到 1K 重试;JSON、multipart edits、Chat 转 Images、`?async=true` 均保留别名;
日志/CSV 将三个 SKU 标为按张计费。

#### 7.5.4 Cutover 与回滚

上述验账全部通过后,在 Mac 先 dry-run,再移除 `#12` 的 canonical:

```bash
node scripts/configure-gpt-image-resolution-skus.mjs \
  --phase=cutover --pricing-snapshot="$PRICING_SNAPSHOT"
node scripts/configure-gpt-image-resolution-skus.mjs \
  --phase=cutover --pricing-snapshot="$PRICING_SNAPSHOT" --apply
```

最终 `/v1/models`、Portal `/models` / `/pricing` 应包含三个固定价别名且不含 canonical;
其他独立图片模型可继续存在。`gpt-image-2-cf` 是另一个上游超分 SKU,脚本刻意不改它的模型或价格;
若产品不希望向客户提供这个独立低价 4K 选项,必须另立变更隐藏/移除,不得悄悄并入本脚本。

任一项不符立即禁用图片组,不允许边放客户流量边修数。手工回滚时先使用脚本输出的快照:

```bash
# 例:先恢复 cutover 快照(重新加回 canonical)
node scripts/configure-gpt-image-resolution-skus.mjs \
  --phase=restore --snapshot='/absolute/path/to/gpt-image-cutover-....json'
node scripts/configure-gpt-image-resolution-skus.mjs \
  --phase=restore --snapshot='/absolute/path/to/gpt-image-cutover-....json' --apply

# 若同时回滚 Portal,再恢复 prepare 快照(移除别名/映射)
node scripts/configure-gpt-image-resolution-skus.mjs \
  --phase=restore --snapshot='/absolute/path/to/gpt-image-prepare-....json' --apply

# 最后恢复 pricing 快照(移除本次新增的三条 ModelPrice)。
# 必须记录这次命令新输出的 gpt-image-restore-....json,
# 不能把下面的 pricing source 快照当成恢复持久化证明。
node scripts/configure-gpt-image-resolution-skus.mjs \
  --phase=restore --snapshot='/absolute/path/to/gpt-image-pricing-....json' --apply
```

`ModelPrice` 恢复后仍是未持久化的 API 状态。在 Mac 对“本轮新生成的 restore 快照”的
`.after.model_price` 求 SHA-256,并在 VPS 用上文同一条 MySQL `SHA2(value,256)` 查询核对。
一致后重启 new-api、等待至少 65 秒,再回 Mac 执行:

```bash
PRICING_RESTORE_RUN='/absolute/path/to/gpt-image-restore-....json'
node scripts/configure-gpt-image-resolution-skus.mjs \
  --phase=verify --snapshot="$PRICING_RESTORE_RUN" --expect=after
```

任何失败路径中,若脚本输出 `ModelPrice durability is still pending`,必须保持图片组禁用。
先在 Mac 对脚本输出的本轮快照 `.before.model_price` 求 SHA-256,再用上文 MySQL 查询核对;
只有两个哈希一致才能重启 new-api、等待 65 秒,然后原样执行脚本输出的
`--expect=before` 命令。任一步未通过前不得声称已回滚。

`restore` 只在当前配置仍等于快照的 after-state 时才写,检测到后续管理员改动会拒绝覆盖。
本方案从未修改 `ModelRatio` / `CompletionRatio`,回滚也不应触碰它们。只有 API 级恢复无法与快照对齐时,
才考虑全库恢复;那会覆盖维护窗口后的用户、Key、余额和日志。

---

## 8. 备份、恢复与回滚

### 8.1 Portal PostgreSQL 恢复

恢复会写生产数据,必须先停止写流量并再次备份现状。基本命令参考:

```bash
gunzip -c /opt/backups/silkroadai-portal/portal-YYYYMMDD-HHMMSS.sql.gz \
  | docker exec -i silkroadai-portal-db psql -U portal silkroadai_portal_prod
```

正式恢复前要确认备份完整、目标库状态、是否需要清库以及 migration 兼容性,不能直接照抄执行。

### 8.2 代码回滚

每次发布前按 4.4 留下的 `rollback-<TS>` 标签可秒级回退,见 4.4.1。以下为无回滚标签时的做法。

首选做法是在 `dev` 创建 revert、跑完整测试、fast-forward 到 `prod`,再按标准流程部署。紧急情况下可在
服务器临时 checkout 已知正常 SHA 并重建,但必须记录 SHA,事后恢复到 `prod`:

```bash
git switch --detach <KNOWN_GOOD_SHA>
docker compose -f docker-compose.prod.yml build portal
docker compose -f docker-compose.prod.yml up -d portal
```

Prisma migration 是 forward-only。代码回滚不会自动回滚 schema;涉及不兼容 migration 时必须制定数据库
恢复方案,不要用 `git reset --hard` 或 `prisma migrate reset` 处理生产事故。

### 8.3 new-api 一致性备份

变更 new-api 价目、渠道或镜像前,必须先停 Portal 流量和 `new-api-old-port`。下列命令会再短暂停正式
`new-api`,使 MySQL dump 与 `/opt/new-api/data` 处于同一静止窗口;完成后自动启动正式容器。

```bash
(
  set -euo pipefail
  umask 077

  BACKUP_TAG="$(date +%Y%m%d-%H%M%S)"
  BACKUP_DIR='/opt/backups/new-api'
  install -d -m 700 "$BACKUP_DIR"

  docker inspect new-api > "$BACKUP_DIR/new-api-${BACKUP_TAG}.inspect.json"
  docker inspect new-api-mysql > "$BACKUP_DIR/new-api-mysql-${BACKUP_TAG}.inspect.json"
  if docker container inspect new-api-old-port >/dev/null 2>&1; then
    docker inspect new-api-old-port > "$BACKUP_DIR/new-api-old-port-${BACKUP_TAG}.inspect.json"
  fi
  test -f /root/new-api.env
  install -m 600 /root/new-api.env "$BACKUP_DIR/new-api-${BACKUP_TAG}.env"
  test -f /opt/silkroadai-portal/.env
  install -m 600 /opt/silkroadai-portal/.env "$BACKUP_DIR/portal-${BACKUP_TAG}.env"

  NEWAPI_STOPPED=0
  restart_newapi() {
    if [ "$NEWAPI_STOPPED" = '1' ]; then
      docker start new-api >/dev/null
    fi
  }
  trap restart_newapi EXIT

  docker stop new-api >/dev/null
  NEWAPI_STOPPED=1
  docker exec new-api-mysql sh -lc 'test -n "$MYSQL_DATABASE" && test -n "$MYSQL_ROOT_PASSWORD"'

  SQL_TMP="$BACKUP_DIR/.new-api-${BACKUP_TAG}.sql.gz.tmp"
  DATA_TMP="$BACKUP_DIR/.new-api-data-${BACKUP_TAG}.tar.gz.tmp"
  docker exec new-api-mysql sh -lc \
    'exec mysqldump --single-transaction --routines --triggers -uroot -p"$MYSQL_ROOT_PASSWORD" "$MYSQL_DATABASE"' \
    | gzip -c > "$SQL_TMP"
  test -s "$SQL_TMP"
  gzip -t "$SQL_TMP"

  test -d /opt/new-api/data
  tar -C /opt/new-api -czf "$DATA_TMP" data
  test -s "$DATA_TMP"
  tar -tzf "$DATA_TMP" >/dev/null

  mv "$SQL_TMP" "$BACKUP_DIR/new-api-${BACKUP_TAG}.sql.gz"
  mv "$DATA_TMP" "$BACKUP_DIR/new-api-data-${BACKUP_TAG}.tar.gz"

  docker start new-api >/dev/null
  NEWAPI_STOPPED=0
  for _ in $(seq 1 30); do
    if curl -fsS --max-time 2 http://172.17.0.1:3000/api/setup >/dev/null; then
      break
    fi
    sleep 1
  done
  curl -fsS --max-time 5 http://172.17.0.1:3000/api/setup >/dev/null
  trap - EXIT

  ls -l "$BACKUP_DIR"/*"${BACKUP_TAG}"*
)
```

`set -euo pipefail` 是必须的:没有 `pipefail` 时 `mysqldump` 失败仍可能由 `gzip` 返回 0,生成能通过
`gzip -t` 但没有 SQL 内容的伪备份。若子 shell 在启动 new-api 前异常退出,`trap` 会尝试恢复;
仍必须人工执行 `docker ps` 和 `/api/setup` 回读。备份验证完成前不得删除 `new-api-old-port`。

---

## 9. 常见故障

### Portal 内部正常,公网超时

依次检查:

```bash
curl -I http://127.0.0.1:3002/login
systemctl status nginx --no-pager
nginx -t
ss -lntp | grep -E ':(80|443)\b'
firewall-cmd --list-services
getent ahostsv4 llmroute.club
```

内部 200 而公网失败通常在 Nginx、云安全组、Cloudflare DNS 或 SSL 模式。

### Portal 连不上 new-api

```bash
curl -sS http://172.17.0.1:3000/api/setup
docker exec silkroadai-portal node -e \
  "fetch('http://host.docker.internal:3000/api/setup').then(r=>console.log(r.status)).catch(console.error)"
docker inspect new-api --format '{{json .HostConfig.PortBindings}}'
docker network inspect new-api_new-api-net >/dev/null
```

重点确认 new-api 绑定 `172.17.0.1:3000`,Portal compose 的 `extra_hosts` 和网络 `gw_priority` 未被删除。

### DNS 解析失败导致 `dnf` 不可用

服务器曾出现公网 IP 可达但域名无法解析。当前连接名通常是 `System eth0`,恢复基线:

```bash
CONN="$(nmcli -t -f NAME con show --active | head -n1)"
nmcli con mod "$CONN" ipv4.ignore-auto-dns yes ipv4.dns "1.1.1.1 8.8.8.8"
nmcli con mod "$CONN" ipv6.ignore-auto-dns yes
nmcli con up "$CONN"
getent hosts mirrors.almalinux.org
```

### SSH 偶发超时或被关闭

```bash
ssh -4 \
  -i "$HOME/.ssh/llmroute_ed25519" \
  -o IdentitiesOnly=yes \
  -o IPQoS=none \
  root@82.29.71.122
```

2026-08-04 已完成 SSH key-only 加固。有效配置位于
`/etc/ssh/sshd_config.d/00-llmroute-hardening.conf`:

```text
PermitRootLogin prohibit-password
PubkeyAuthentication yes
PasswordAuthentication no
KbdInteractiveAuthentication no
MaxAuthTries 3
LoginGraceTime 30
```

原生 OpenSSH 和 Xterminal 已验证可用;FinalShell 4.6.5 使用相同 RSA key 仍在 preauth 阶段断开,
属于客户端兼容/配置问题,不要因此重新开放服务器密码登录或全局启用旧 `ssh-rsa` 算法。主机 ED25519
指纹为 `SHA256:ZcqH0BmGvCJ33HsAiTkuFT0cLENcM2UDdz3lzVwThB0`,首次使用新客户端时必须先核对再保存。

`fail2ban` 配置位于 `/etc/fail2ban/jail.d/sshd.local`:10 分钟内失败 5 次先封 1 小时,重复触发按 2 倍
递增,最长 24 小时。检查状态:

```bash
systemctl is-active sshd fail2ban
fail2ban-client status sshd
sshd -T | grep -Ei \
  'permitrootlogin|pubkeyauthentication|passwordauthentication|kbdinteractiveauthentication|maxauthtries|logingracetime'
```

服务器曾累计观察到 2,236 次公网失败尝试。私钥不得上传仓库或聊天;丢失全部授权私钥时,只能通过
云厂商控制台恢复。修改 sshd 前仍必须保留一个已登录会话,并先运行 `sshd -t`。

### SSH 隧道首个请求成功、加载后台后全部卡死

2026-08-04 在 operator 当前中国电信网络上复现并定位过一次。症状是 `/api/setup` 首次返回 200,
但加载 new-api 的 3.42 MB 主脚本后,后续 curl 虽能连接本地监听却收不到响应。该问题不是 Chrome、
new-api、Docker、SSH forwarding 或 MTU:

- 不经过端口转发、直接让 SSH shell 下发 5 MB 数据时同样卡住,耗时 5 分 32 秒。
- 直接从 VPS Nginx HTTP/80 下载 5 MB 同样超时,证明并非仅 SSH/22 被限制。
- 反方向 Mac -> VPS 上传 5 MB 约 11 秒,故障集中在 VPS -> Mac 下行。
- `ss -tinp` 显示服务端发送数据重传约 17%-28%,CUBIC `cwnd` 塌到 2-4;同时 MSS 1440、PMTU
  1500 正常。路由追踪显示该方向经海外运营商进入中国电信,但仅凭 traceroute 不能断言具体故障跳点。

8 月 4 日的历史测试中，临时切换 BBR 后，同一 SSH 5 MB 下行从 5 分 32 秒降到 8.29 秒，
new-api 3.42 MB 主脚本在新隧道中 3.91 秒完整下载。这些数值不是本次验收结果。

2026-09-11 再次排查时，发现 `/etc/sysctl.d/99-llmroute-bbr.conf` 实际是目录，内嵌配置未按
预期加载，运行值为 `cubic` / `fq_codel`。旧记录“已持久化 BBR + fq”不准确；目录时间早于
本次应用发布，不能据此归因于发布。此次已备份原目录并修成普通配置文件，仅持久化已验证有效的
BBR，保留 `fq_codel` 和全部活动网卡队列，没有运行全局 `sysctl --system`。

同一 110270 字节压缩脚本的三轮交替对照：旧 CUBIC 隧道中位耗时 6.558 秒，独立 BBR 隧道
0.827 秒。正式 3000 隧道重建后中位耗时 0.916 秒，主脚本 978628 字节下载 1.165 秒。
这是当前线路的短时测量，不保证所有页面提速同样倍数。完整证据和回退见
[`docs/SSH-TUNNEL-REPAIR-2026-09-11.md`](../docs/SSH-TUNNEL-REPAIR-2026-09-11.md)。当前配置为:

```text
# /etc/modules-load.d/llmroute-bbr.conf
tcp_bbr

# /etc/sysctl.d/99-llmroute-bbr.conf
net.ipv4.tcp_congestion_control = bbr
```

检查当前状态:

```bash
lsmod | grep '^tcp_bbr'
sysctl net.ipv4.tcp_congestion_control \
  net.ipv4.tcp_available_congestion_control \
  net.core.default_qdisc

# 将 IP 换成 operator 当前公网 IP;关注 bytes_retrans、cwnd 和 bbr 字样
ss -tinp state established dst <OPERATOR_PUBLIC_IP>
```

预期为 `tcp_congestion_control = bbr`、`default_qdisc = fq_codel`；还须根据本机 SSH 的源端口
定位具体 TCP 连接，确认 `ss -ti` 显示 `bbr`，不能仅检查全局默认值。
已验证定向加载及 `systemd-analyze cat-config sysctl.d` 的最终配置为 BBR；本次未重启服务器，
没有进行整机重启后的验收。线路本身仍可能丢包，BBR 是已验证的缓解措施；若再次退化，先重新取证。

---

## 10. 运维速查

```bash
# 登录
ssh -4 -i "$HOME/.ssh/llmroute_ed25519" -o IdentitiesOnly=yes -o IPQoS=none root@82.29.71.122

# Portal 状态与日志
cd /opt/silkroadai-portal
docker compose -f docker-compose.prod.yml ps portal portal-postgres
docker logs -f silkroadai-portal

# new-api 状态与日志
docker ps --filter name=new-api
docker logs -f new-api

# Portal DB shell
docker exec -it silkroadai-portal-db psql -U portal silkroadai_portal_prod

# Nginx
nginx -t
systemctl reload nginx

# Navicat 必须通过 SSH 隧道
# PostgreSQL host=127.0.0.1 port=5432,SSH host=82.29.71.122 port=22
```

Portal 管理后台:`https://llmroute.club/admin/login`。它与 new-api 后台是两套账号系统。

---

## 11. 上线清单

- [ ] 上游变更已先报告,再合入 `dev`。
- [ ] `dev` typecheck、完整测试、格式检查通过。
- [ ] `prod` 仅通过 fast-forward 更新并已 push。
- [ ] VPS Git 工作区干净,当前分支为 `prod`。
- [ ] `.env` 已备份,新变量已补齐,secret 未打印或提交。
- [ ] `NEXT_PUBLIC_APP_URL`、`NEXT_PUBLIC_API_URL`、`NEXT_PUBLIC_IMAGE_URL` 与客服公开变量已写入生产 `.env`。
- [ ] `NEWAPI_CUSTOMER_BASE_URL=http://127.0.0.1:3002` 已写入生产 `.env`。
- [ ] `NEWAPI_QUOTA_PER_USD` 与 new-api `QuotaPerUnit` 一致;计费单位迁移后
      `USD_TO_CNY_RATE=1` 且 `REAL_USD_TO_CNY_RATE` 已显式设置。
- [ ] Portal PostgreSQL 已备份且 `gzip -t` 通过。
- [ ] 备份后当前 shell 已恢复 `umask 022`;未在 `umask 077` 下执行 `git pull` 或构建。
- [ ] 宿主机 `prisma/` 权限检查和 runner 一次性只读检查均通过;新增 migration 对 `nextjs` 可读。
- [ ] **build 之前**已给线上旧镜像打 `rollback-<TS>` 标签并核对 ID 一致;build 后新镜像已打 `release-<SHA>`。
- [ ] `new-api_new-api-net` 存在,new-api 内外链路可达。
- [ ] 只构建并启动 `portal`,未启用 `api-replicas` 或 `seedance` profile。
- [ ] migration 完成,无失败日志。
- [ ] apex 与 `www` 的内部源站、Cloudflare 公网检查通过。
- [ ] API-only Nginx virtual host 已安装,`/v1`、`/v1beta`、CORS 和 SSE 验收通过。
- [ ] API 子域的 `/login`、`/admin/login` 均为 404,主站/API access log 都不含查询参数。
- [ ] 主站公网 `/image-adapter/ominiapi/v1/images/generations` 返回 404。
- [ ] Google、GitHub 各完成一次真实浏览器登录。
- [ ] 支付相关发布完成一笔受控小额充值并确认幂等入账。
- [ ] SSH key-only 新会话可登录,密码认证仍为关闭,fail2ban `sshd` jail 为 active。
- [ ] SMTP、R2、Sentry、Tavily 等未完成项仍明确标记,没有误报上线。
- [ ] 记录本次线上 SHA、部署时间、验证结果和回滚 SHA。

## 2026-09-08 上游同步的下次发布前置项（尚未发布）

本轮只集成 dev，prod 尚未推进。详情见 `docs/UPSTREAM-SYNC-2026-09-08.md`。

- 修正联机验证目标并通过完整测试后，才能 fast-forward dev → prod；VPS 发布仍需 operator 明确安排。
- 安装仓库 `deploy/nginx/llmroute-web.conf` 并先验证 `nginx -t`，再启动新 Portal 镜像：新增 `/minimax-adapter/`、`/seedream-adapter/` 必须与 `/image-adapter/` 一样公网 404，供 new-api 使用共享 Docker 网络内部地址。
- 本轮 6 条 migration 先于新镜像启动应用；合法历史拓扑升级到 76 条已在 PostgreSQL 16 隔离库验证。保持现有 Docker 网段，不套用上游固定 172.21.0.0/16。
- 保持 `PORTAL_BATCH_ENABLED=false`；当前实现不具备收费安全的崩溃恢复和凭据保护。不要运行上游 Seedream setup 脚本或开启 api-replicas/seedance profile 来完成常规发布。

## 2026-09-09 上游同步追加前置项（尚未发布）

本轮 #449–#451 继续在 dev 集成，operator 已明确暂停上线。报告见 `docs/UPSTREAM-SYNC-2026-09-09.md`。

- 公网主站新增 `/image-adapter25/` JSON 404；旧 `/image-adapter/` 前缀无法覆盖它。下次发布必须先安装更新后的 `deploy/nginx/llmroute-web.conf` 并通过 `nginx -t`，再启动带新路由的镜像。共享 Docker 网络内部地址为 `http://silkroadai-portal:3002/image-adapter25/wetokenasia25`；本轮未配置该渠道。
- 2.5 图片适配器与原图片适配器独立；支持的代码白名单为 `gpt-image-2.5-flare`、`gpt-image-2.5-sunburst`。供应商、价格和真实链路未验收；输入 mask、WebP 判定、多图不同尺寸计费及失败恢复等限制见报告，不可直接宣布模型已上架。
- Seedance 2.5 的 1080p 基准改为无视频 ¥77 / 含视频 ¥46（每百万 token）。任务完成时按代码当前费率结算，已结算历史任务不重算；下次发布需注意在途任务的调价边界。本轮未修改 new-api 渠道价格或客户折扣。
- 国内 2.5 的 480p 默认上游名改为 `doubao-seedance-2-5-260628`；若生产显式设置 `SEEDANCE_XHK_MODEL_25_480P`，环境覆盖仍优先，发布前需核对。720p/1080p 默认保持 `artsdance-2-5-pro-260801`。
- 本轮无新增 migration/env/依赖/Compose 变化，上轮的 6 条待发布 migration、Batch 关闭和单 Portal 发布规则继续有效。当前完整测试仅联机模型列表 smoke 失败（本机 3000 返回 HTML），prod 保持 `02cbf26`。

### 同日发布准备后续（仍未部署）

- operator 已恢复 SSH 隧道；完整测试以 `NEWAPI_BASE_URL=http://127.0.0.1:3000` 运行并通过：287 files / 3680 passed / 1 既有 skipped，原三项 smoke 全部通过。使用实际隧道的 IPv4 地址，避免 `localhost` 命中 HBuilderX 的 IPv6 服务。可用 `pnpm test:smoke` 单独检查；它先无凭据验证状态结构，再进行真实模型列表鉴权。
- 后续本项目改动新增 `PORTAL_IMAGE_ADAPTER25_ENABLED=false`。上线时它和 `PORTAL_BATCH_ENABLED=false` 都必须保持关闭；变量未设置也默认关闭。生产模板已写明，实际服务器 `.env` 尚未改动。
- 新适配器内部问题尚未修复，开关不能替代供应商/计费验收。完整测试通过也不代表 6 条 migration 已在生产执行。
- operator 的暂停指令继续有效，prod 仍为 `02cbf26`。正式安排上线后按本手册完成备份、迁移、Nginx 隔离与切换后检查；证据和发布范围见 `docs/RELEASE-READINESS-2026-09-09.md`。

## 2026-09-09 正式发布（已完成）

operator 随后明确安排上线，运行代码 `prod@d3c60d3` 于北京时间 15:33 发布。后续记录提交仅更新文档，不重建镜像；详细证据见 `docs/DEPLOY-2026-09-09.md`。

- `.env` 备份为 `.env.bak.20260909-072006`；PostgreSQL 备份为 `/opt/backups/silkroadai-portal/portal-20260909-072006.sql.gz`（0600、gzip 通过）；Nginx 备份和状态记录在 `/opt/backups/silkroadai-portal/releases/20260909-072006/`。
- 新镜像 ID 为 `sha256:04126eede7e98d51848700b95a9242170583d2f48efb384e4c51efdf2f9572e5`；旧镜像保留标签 `silkroadai-portal-portal:rollback-20260909-072006`，回滚代码 `02cbf26`。
- 六条新增 migration 全成功，76 applied / 0 unfinished / 0 rolled back，历史与新增 checksum 均一致；四类内部适配器路径的主站/www/API 源站及公网隔离均通过。
- Portal restart count 0、启动后日志无 error；PostgreSQL healthy，PostgreSQL/new-api/MySQL 容器 ID 与启动时间未变。切换前无 Portal 入站 established 连接、无未结算视频任务；本机探测约 6 秒短暂不可用后恢复。
- 50 项网络检查与真实 Key `/v1/models` 通过（该档 4 个模型），12/12 非 JWT 持久用户 token 鉴权通过；固定图片价 ¥1/¥1.5/¥2 未变，动态拓扑不变量全为 0。
- 生产显式设置 `PORTAL_BATCH_ENABLED=false`、`PORTAL_IMAGE_ADAPTER25_ENABLED=false`，入口 503 和空 Batch 队列均核实。新供应商、收费推理、支付和真实浏览器 OAuth 登录没有在本次重复验收。
- Gemini CORS 验收针对无 Cookie 的 `x-goog-api-key` 模式；new-api 返回的 header 通配符不能扩大解释为任意凭据模式。Cloudflare 仍会对默认 Python-urllib 返回 403/1010，本次未修改其规则；标准 curl 的假 Key 为 401，真实 Key 为 200。两项原始验收失败和复核依据均保留在发布记录。

## 2026-09-11 正式发布（已完成）

operator 明确安排后，运行代码 `prod@e69f225` 于北京时间 10:54 发布；后续记录提交仅更新文档，不重建镜像。详见 `docs/DEPLOY-2026-09-11.md`。

- 发布内容为渠道替换预览与上游 #452–#458；无 migration/schema/依赖/Compose/Nginx 新改动，生产 `.env`、Cloudflare 和两个关闭开关均保持原状。
- `.env.bak.20260911-024813` 与 `/opt/backups/silkroadai-portal/portal-20260911-024813.sql.gz` 为 0600，gzip 完整；Nginx 和发布证据在 `/opt/backups/silkroadai-portal/releases/20260911-024813/`。
- 镜像为 `sha256:1d607b258ab6826d8fafadcc6b6ac6e241650bec7ed79eed09103a3332cfafe1`；回滚标签为 `silkroadai-portal-portal:rollback-20260911-024813`，Git 回滚点 `65a99e5`，实际旧应用 `d3c60d3`。
- Git 归档排除全部 `.env*`，旧服务运行期间完成构建；一次性非 root 读取与 PostgreSQL 16 migration status 检查通过，76 条 checksum 全部匹配，无 pending。
- 切换前入站 established 连接和未结算视频均为 0；监测约 6 秒短暂中断。Portal restart count 0、日志无 error，三个依赖容器均未重启。50 项网络与只读业务验收通过。
- 默认 urllib 真实客户 Key 模型列表为 200，9/9 发布后独立完成的 Cloudflare 兼容修复继续有效。渠道替换 GET/dryRun 通过，未执行真实替换；CCMax 登记 `[14]` 与 10 模型引用不变。
- 无收费生成、支付或真实浏览器 OAuth 登录。未来若产生 `cgt-recovery-` 视频任务，回滚旧镜像前须保留解码兼容至终态；不得覆盖恢复生产库或删除任务映射。

## 2026-09-11 17:25 目录同步发布（已完成）

operator 明确「上线！」后，`dev@c1d9501` fast-forward 至 prod 并发布。完整记录见
`docs/DEPLOY-CATALOG-SYNC-2026-09-11.md`；后续文档提交不重建应用镜像。

- 镜像为 `sha256:9d61a61fcec28b1cfe6bdaf2fcc3f32cdb844aacbb91a32a7bddc053d7177d07`，标签
  `silkroadai-portal-portal:release-c1d9501`。从排除 `.env*` 的 Git 归档构建，无 migration/env/
  依赖/Nginx/Cloudflare 变化，76 条 migration 全部匹配，Image 2.5 / Batch 保持关闭。
- `.env.bak.sync-20260911-091532` 与发布目录
  `/opt/backups/silkroadai-portal/releases/sync-20260911-091532/portal.sql.gz` 均为 0600，gzip 通过。
  同目录保存 Nginx 归档、镜像/运行记录和隔离库验收证据。回滚标签为
  `silkroadai-portal-portal:rollback-sync-20260911-091532`，旧 Git `86cb84e`，实际旧应用 `e69f225`。
- 同镜像 + PostgreSQL 16 隔离库的 24 项验收通过，确认整批写入、真实中途失败回滚、重放和租户隔离。
  临时库/容器/含密环境文件已清理。生产仅预览同步，未确认改价，原 5 个价格版本与全部目录/Key 不变。
- 切换北京时间 17:25:15–17:25:30，采样约 7 秒短暂中断；Portal restart count 0，日志无 error。
  PostgreSQL/new-api/MySQL 容器 ID 与启动时间未变；12/12 持久 token、真实 Key 模型列表与生产预览通过。
- 50 项源站/公网与 45 项只读业务检查通过。Gemini CORS 仅按无 Cookie 的 `x-goog-api-key` 模式验收，
  `Allow-Headers: *` 未扩大解释为带 Cookie 的任意凭据模式；检查脚本首轮误判与修正记录均保留。

## 2026-09-12 定价发布改造（待发布）

本次仅开发验证，生产继续运行 `c1d9501`。实现、限制与验收见
[改造报告](../docs/PRICING-PUBLISH-2026-09-12.md) 和
[隔离数据库验收](../docs/PRICING-PUBLISH-DB-VERIFICATION-2026-09-12.md)。以下是 operator 安排上线后执行的前置项，不能当作已经完成。

1. 发布前备份 Portal PostgreSQL、生产 `.env` 及 new-api 当前价格选项；核对没有其他后台或脚本同时修改 new-api 价格。按既有流程从干净 Git 归档构建，只切换 `portal`。
2. 新增 `20260912040000_add_pricing_publications`，创建任务、协调器、写入日志三表。先执行 migration，再启动新镜像；即使不配置价格发布调度器，目录写入口也需要协调表。不能对 `prisma/newapi-readonly.prisma` 执行 migrate/db push，该 schema 只生成只读客户端。
3. 在实际 new-api MySQL 数据库创建专用账号，仅授予该数据库 `options` 表的 `SELECT`。不要复用 root/new-api 写账号。`NEWAPI_PRICING_DATABASE_URL` 指向容器内可达的 MySQL 地址及真实库名，账号密码仅进入生产 `.env`，不输出到日志。保留当前 `new-api_new-api-net` 和 Portal 默认网关优先级，不发布 MySQL 公网端口。
4. 优先使用 `?ssl=true` 并验证服务端证书；自签 CA 须事先可信地安装，可用 `NODE_EXTRA_CA_CERTS` 指向只读挂载中的 CA。禁止关闭证书校验。若现有 Docker 内网使用非 TLS 连接，则从受信任的服务器渠道取得并核对 MySQL RSA 公钥，设置 `NEWAPI_PRICING_RSA_PUBLIC_KEY_FILE=/app/newapi-pricing/mysql-public-key.pem`。固定 RSA 公钥只保护鉴权交换，不能代替查询传输加密；不得用于不可信网络。
5. Compose 已将宿主机 `/opt/silkroadai-portal/deploy/runtime/newapi-pricing` 只读挂载到 `/app/newapi-pricing`。发布前建立目录并放入已核验的公开信任材料，建议目录 0755、文件 0644，确保容器 uid 1001 可读；不得放入密码、私钥或 `.env`。该目录已被 Git 与 Docker 构建忽略，应用禁止自动从网络取得陌生 RSA 公钥。
6. PostgreSQL 连接池须至少有两条可用连接并预留正常请求余量：发布事务占一条，独立持久写入日志占另一条。调度器仅在配置只读连接时启动，并继续遵守 `PORTAL_SCHEDULERS=off` 和 Enterprise 实例隔离。构建/CI 使用 `pnpm prisma:generate` 生成两个客户端，生成不需要真实数据库连接。
7. 切换后先执行不改价的核验：管理员 API 和只读 MySQL 的四个价格选项一致；预览明确显示模型、全部受影响档次及单位；缺少核验连接时预览失败且没有 PUT。首次真实发布须由 operator 指定模型和金额，不能以隔离测试代替实际 new-api 二进制的验收。真实改价时记录任务 ID，并检查最终 `succeeded`、双读目标值、全部目录行及 SDK 目录刷新。
8. 两项倍率 PUT 没有 new-api 事务保障，其间客户请求可能短暂按混合倍率计费；当前不会暂停请求。回滚应用镜像不会恢复已经改变的 new-api 价格。存在活动任务或 `in_flight` 日志时，不得直接启动会绕过协调锁的旧应用或用旧全表快照覆盖配置；先按 [未决写入恢复](../docs/PRICING-PUBLISH-UNCERTAIN-WRITES.md) 核实请求终态并处理任务，保留三表和审计记录。

本轮没有创建生产数据库账号、改生产环境变量、迁移生产数据库、重启服务或发布真实价格。生产实际账号权限、连接信任和首次受控发布仍待验收。

### 同日正式发布（12:58 已完成）

operator 随后明确批准上线，运行应用为 `prod@becf7f5`；详情见
[定价发布上线记录](../docs/DEPLOY-PRICING-PUBLISH-2026-09-12.md)。上节为开发阶段的历史前置项，以下为实际已完成状态。

- 已配置仅 SELECT `new_api.options` 的 `portal_pricing_ro@172.18.%` 和固定 RSA 公钥只读挂载；生产仅追加两个 reader 环境变量。MySQL 自动证书没有可验证主机名，本次沿用可信私有 Docker 网络、固定公钥鉴权，未启用 TLS 或重启 MySQL，不扩大公开端口。
- 真实 Node 22.23.2 Linux 镜像、生产备份克隆的 13 项预验收通过后，先迁移生产三表，再切换单 Portal。生产77条migration全成功且checksum匹配，目录和远端价格未变；12:57:53–12:58:08切换，首次失败至恢复约9秒，restart count 0，依赖容器未重启。
- 备份目录 `/opt/backups/silkroadai-portal/releases/pricing-20260912-034121/`，旧应用 `c1d9501`、旧镜像标签 `silkroadai-portal-portal:rollback-pricing-20260912-034121`。新镜像标签 `silkroadai-portal-portal:release-becf7f5`，后续文档提交不重建镜像。
- 新版 Kimi 只读预览、四项价格双读、12/12持久token和管理员现有Key两类UA公网模型列表通过。jobs/writes=0；尚无真实价格发布。4个GPT主型号已有阶梯计费，通用普通改价会拒绝覆盖。
- 首份候选发现rc.23缺少两个新版可选配置项，已在dev修复后重新验证/构建；核心价格核验未放宽。50项网络检查经5项本机超时复查补齐，未修改Nginx/Cloudflare。

### 同日定价入口修正（16:19 已完成）

应用 `ad8594a` 仅统一各档次“改价并发布”入口，移除首行重复按钮，后端不变；镜像标签 `silkroadai-portal-portal:release-ad8594a`。备份目录 `/opt/backups/silkroadai-portal/releases/pricing-ui-20260912-081339/`，回滚标签 `rollback-pricing-ui-20260912-081339`（旧应用 `becf7f5`）。77 条 migration、价格/目录/Key 摘要与 new-api 选项不变，无活动发布任务；依赖未重启。公网新 JS hash 已与镜像核对，详情见 [定价发布记录](../docs/DEPLOY-PRICING-PUBLISH-2026-09-12.md)。后续纯文档提交不重建镜像。

### 同日逐档历史整理（16:50 已完成）

应用 `d718acd` 统一每档“改价并发布 / 历史”，历史逐档展开，退役档保留只读审计，后端不变。镜像标签 `silkroadai-portal-portal:release-d718acd`；备份目录 `/opt/backups/silkroadai-portal/releases/pricing-history-20260912-083750/`，回滚标签 `rollback-pricing-history-20260912-083750`（旧应用 `ad8594a`）。77条migration正常，价格/目录/Key/new-api计费选项不变，发布jobs/writes为0；公网新JS hash核对通过。后续纯文档提交不重建镜像，详细交互和上线证据见 [定价发布记录](../docs/DEPLOY-PRICING-PUBLISH-2026-09-12.md)。

## 2026-09-14 上游 #459–#464 发布（10:53 已完成）

运行应用为 `prod@4413e61`，镜像标签 `silkroadai-portal-portal:release-4413e61`、镜像 ID `sha256:6c8d7e47ec47f11a7e525fd8fc4aab729c7b01dee6f337c8434b1ba671237b58`。从不含 `.env*` 的干净归档构建，先使用生产备份克隆库验收真实页面、77 条 migration 与只读价格预览，再仅切换 Portal。后续纯文档提交不重建镜像；无 migration/env/Nginx/Cloudflare 增量。

- 备份目录 `/opt/backups/silkroadai-portal/releases/upstream-20260914-022302/`，`.env` 备份 `/opt/silkroadai-portal/.env.bak.upstream-20260914-022302`；文件0600、gzip/隔离恢复验证成功。
- 旧 Git `b092641`，回滚镜像标签 `silkroadai-portal-portal:rollback-upstream-20260914-022302`。本次未触发回滚。今后回滚仍须先处理非终态发布任务和 in_flight 写入，不能以恢复旧镜像代替远端价格恢复。
- 10:53:24–10:53:39 切换，首次失败至恢复采样9.425秒；Portal restart count 0，PostgreSQL/new-api/MySQL未重启。77条迁移与checksum正常，启动日志读取成功且无error/fatal/failed。
- 50项源站/公网检查全部通过；13/13用户持久token鉴权、管理员已有Key两种UA模型列表、实际价格页与Kimi只读预览通过。29模型、35价格版本、6档次、21Key及远端价格选项不变；固定图价¥1/¥1.5/¥2不变，发布jobs/writes/active/in_flight均0。
- new-api专用SELECT-only reader与固定RSA公钥保持，仍按既有可信Docker内网运行，不冒称TLS。Image2.5/Batch显式false且实际入口503；没有收费生成、真实改价、支付或完整浏览器OAuth登录。

详细证据、首轮候选检查条件纠正及发布范围见 [2026-09-14 发布报告](../docs/DEPLOY-2026-09-14.md)。

## 2026-09-15 上游 #465 发布（09:22 已完成）

- operator 明确「上线吧」后，已发布 `prod@047e64c`；运行镜像 `sha256:0efc20216cc25f8585668d752f842621c27e5cb40f9cd62892e277f50f712ca9`。后续纯文档提交不重建镜像。
- 备份目录 `/opt/backups/silkroadai-portal/releases/upstream-20260915-011013/`；环境备份 `.env.bak.upstream-20260915-011013`，旧 Git `2c84623`，回滚标签 `silkroadai-portal-portal:rollback-upstream-20260915-011013`。真实隔离恢复与 14 项候选检查通过，临时资源已清理。
- 切换源站采样中断 8.124 秒后恢复；Portal restart count 0、日志 error 为 0，PostgreSQL/new-api/MySQL 未重启。77 条 migration checksum 正常，生产环境变量、价格、目录、Key 保持不变，发布 jobs/writes/active/in_flight 为 0。
- 50 项网络检查、13/13 用户持久鉴权、两类 UA 模型列表、真实价格页和 Kimi 只读预览通过。Image 2.5/Batch 保持显式 false；没有真实改价、收费生成或支付。
- 运维 SSH 使用三个隔离参数，不复用或创建用户隧道 master；正式 Portal 调度器未关闭。完整证据与范围见 [发布报告](../docs/DEPLOY-2026-09-15.md)。


## 2026-09-15 成本定价工作台发布（14:06 已完成）

- 按 operator「做好并测试通过后直接上线」的持续授权发布 `prod@b39681d`；镜像 `sha256:69896fe3eacebec7c3acc8b81ad65dc5fe1ef8c1e8dd967722f4399b58a37382`。文档归档不重建镜像。
- 备份目录 `/opt/backups/silkroadai-portal/releases/cost-pricing-20260915-033010/`，环境备份 `.env.bak.cost-pricing-20260915-033010`；gzip和真实隔离恢复通过。回滚标签 `silkroadai-portal-portal:rollback-cost-pricing-20260915-033010`，旧应用047e64c。
- 候选使用随机克隆库、关闭调度器、无主机端口、只读new-api代理；17项主检查及成本保存/历史/v2混合预览通过并清理。随后生产77→78迁移，只新增成本两表和历史保护；14:05:56–14:06:11切换Portal，约7秒短暂不可用，restart0，依赖未重启。
- 50项网络检查、13/13持久鉴权、两类UA模型列表、成本GET、价格双读及前后摘要通过。现有模型/价格/档次/Key不变、成本两表为空、发布任务/写意图为0，日志error0；未实际改价或收费。生产调度器正常开启，Image2.5/Batch继续false。
- 回滚前必须核实非终态v2任务、活动协调器和不确定写入全部处理完成，不能让旧worker接管v2；新增成本表及历史保留。详见 [成本定价发布报告](../docs/DEPLOY-COST-PRICING-2026-09-15.md)。

## 2026-09-15 Portal 分组一键清理发布（16:10 已完成）

- 按持续授权发布 `prod@04868d4`；镜像 `sha256:78480acc3fe4ada4ac5cde3e3ea3ff39300b5d6f6be1a9e0d6ec36f9bffd2bf4`。后续纯文档归档不重建镜像。本轮无 migration/env/依赖/Docker/Nginx/Cloudflare 变化，仅切换 Portal。
- 备份目录 `/opt/backups/silkroadai-portal/releases/group-retire-20260915-075852/`，库备份 `portal.sql.gz`，环境备份 `/opt/silkroadai-portal/.env.bak.group-retire-20260915-075852`；回滚标签 `silkroadai-portal-portal:rollback-group-retire-20260915-075852`，旧运行源码 `b39681d`。回滚仍须核实价格非终态任务、活动协调器和不确定写入，恢复应用不会撤销管理员已确认的数据清理。
- 生产备份克隆库通过 19 项候选检查，并经真实 HTTP 完成分组清理/模型下架/默认档替代，验证价格、成本历史、Key、白名单及倍率记录不变。候选通过只读代理访问 new-api，临时容器、库与含密文件清理已确认。
- 北京时间16:10:43–16:10:58切换，监测25样本/12次失败，失败样本首尾跨度7.92秒（约8秒观测失败窗口）；Portal restart count 0、启动日志error0，PostgreSQL/new-api/MySQL身份、镜像及启动时间保持。78条migration全部完成且checksum一致，无新增迁移。
- 新入口生产只做 `action=preview`：匿名401、管理员200；当前6个分组未改，「GPT-特惠反代」预览影响7模型、其中下架1，既有15 Key/14 active，来源present。九表、用户白名单及环境摘要不变，发布jobs/writes/active/in_flight为0。没有生产 `action=apply`、改价或收费。
- 50/50源站公网、13/13持久token身份鉴权、13/13 new-api计费、两类UA模型列表（均200/4模型）通过，拓扑异常0，3条报价0mismatch，固定图目录/new-api仍¥1/¥1.5/¥2；21个活动模型完整显示，Image2.5/Batch关闭入口503。
- prepare备份脚本的MySQL CLI默认latin1，JSON_OBJECT里的中文被替换，6项raw hash因此与UTF-8驱动不同；现场SQL二进制SHA与驱动raw一致，未发现价格差异。旧有损备份不得用于覆盖/恢复配置；四主价已通过切换前候选无损canonical摘要与发布后的对比，附加中文配置仅有同CLI方法的前后等价证据，不宣称所有选项逐字节不变。
- 今后的MySQL价格快照CLI必须显式指定 `--default-character-set=utf8mb4`，并建议同时记录数据库端 `SHA2(CAST(value AS BINARY),256)`，将数据库原始值摘要与序列化快照分开核验，避免字符集转换造成误判。本次保留历史快照与历史验收记录，追加该发现和证据限制，不重写旧证据。
- 分组清理只作用于Portal：移除目标tier映射，空映射启用模型下架，保留历史、Key、客户权限与倍率。默认替代必须在预览中确认；仅有该档权限的客户需另行分配可用档次，不能自动清空白名单。完整证据与审计工具问题见 [分组清理报告](../docs/CHANNEL-GROUP-RETIREMENT-2026-09-15.md)。


## 分组删除同时撤销客户 Key（2026-09-16，09:29 已上线）

- 按持续授权发布 `prod@fd595f4`；镜像 `sha256:acd88e3dd1799f7c483c0dc4792e3385bf941ee3af0c9bbe13d1057ef300e858`，后续纯文档提交不重建。分组删除现在先持久化任务、逐把核对客户/token/group并撤销，管理记录缺失且旧凭据拒绝才确认；全部确认后才清理本档模型关联。其他分组/租户、余额及历史保留。
- 新migration `20260916010000_add_channel_group_retirement_jobs` 仅增加任务与Key状态两表；任务不存原始凭据。未完成任务与建Key、注册/OAuth首Key、迁组、目录和价格发布协调；平台历史null用户纳入，其他租户严格隔离。超时、缓存仍接受和远端成功后SQL失败可恢复；未知远端结果不能停止释放锁或回滚旧应用。
- 320 files / 4545 passed / 1既有skipped，typecheck/Prisma/构建/格式通过，lint0 error/93既有warnings。实PG协调7项、任务10项、注册/OAuth真实P2002故障9组核查及CUA流程通过；生产备份克隆22项通过，零Key真实HTTP任务完成后临时资源清理。真实客户Key撤销未作为上线测试执行。
- 备份目录 `/opt/backups/silkroadai-portal/releases/group-key-revocation-20260916-011953/`；环境备份 `.env.bak.group-key-revocation-20260916-011953`；回滚标签 `silkroadai-portal-portal:rollback-group-key-revocation-20260916-011953`（旧应用04868d4）。生产79条migration全部完成，09:29:42–09:29:58切换，失败样本跨度7.92秒；Portal restart0/日志error0，依赖未重启。
- 50/50网络检查、13/13持久授权、两类UA模型列表200/4模型、API/MySQL价格双读通过。九张既有业务表/客户白名单/环境保持；29模型35价格版本5分组21Key、11项配置UTF-8原始字节不变。历史已删除档次仍有14把active Key，为已知遗留状态，不得误报拓扑异常全0。
- 新入口生产仅preview，2把目标Key均ready、默认替代保护正常；两张新任务表为空，未撤销任何生产Key。已删除的GPT特惠分组遗留Key范围问题尚未获答复，不自动批量撤销。入口为「删除任务与遗留 Key」；详细证据见 [功能与发布报告](../docs/CHANNEL-GROUP-KEY-REVOCATION-2026-09-15.md)。

## 遗留 Key 自动识别（2026-09-16，11:24 已上线）

- 运行源码 `prod@cb8aa91`，镜像 `sha256:c017bf2edc931a77a4fe00001b9d93a2ab0cb9da277d9496d8285704538812f5`。无数据库增量，79条migration保持；纯文档归档不重建镜像。
- 遗留清理改为自动预览当前Key归属，不要求回忆已删除new-api group；全记录缺失只归档，签名/恢复严格绑定archiveOnly且不发DELETE。归属冲突、其他启用档占用、鉴权未知及缓存仍接受均不自动通过。未知原名在公开响应为null，数据库归档任务的空group不能解释为default。
- 首轮候选被平台固定UUID校验挡住，未切换，证据保留在 `orphan-auto-20260916-030356`。修复后完整4639项通过，实PG7场景与最终克隆23项通过；最终备份目录 `/opt/backups/silkroadai-portal/releases/orphan-auto-20260916-031652/`，环境备份同后缀，回滚镜像标签 `rollback-orphan-auto-20260916-031652`（旧运行fd595f4）。未完成或未知撤销任务仍禁止盲目回退旧版本。
- 11:24:06–11:24:21切换Portal，失败采样跨度8.424秒后稳定，restart0/日志error0，依赖未重启。50/50网络、13/13持久授权、价格双读和业务摘要保持；真实自动预览识别GPT特惠15Key（14ready、1already_absent），canApply=true。真实清理未执行，生产任务两表0，14活动遗留Key仍保留。
- 操作：刷新渠道分组 → 删除任务与遗留 Key → 对目标点击「预览清理」→ 核对自动识别组名和Key清单 → 确认。正常新删除任务仍先撤销再清理，历史和余额保留。详细限制与证据见 [自动识别发布报告](../docs/ORPHAN-KEY-AUTO-DISCOVERY-2026-09-16.md)。

## 直接填写售价倍率（2026-09-16，14:55 已上线）

- 运行源码 `prod@79c0928`，镜像 `sha256:2aa51d55e7b724b944c6c96d815a00281378f2f1cc1a1d347367c334c3e21ea4`。单项和批量成本定价改为直接填目标售价倍率；上游1.3、售价1.6显示加0.3倍，以基础报价直接计算，旧百分比规则保持精确兼容。保存仍只写成本草稿，实际价格须预览确认发布。
- 无migration/env/Nginx/Cloudflare变化。完整4675项测试、类型/Prisma/构建通过，lint0 error/93既有warning；生产备份克隆22项、真实成本保存/两版历史/签名预览通过。14:55:09–14:55:25切换，失败采样跨度9.429秒；Portal restart0、日志error0，依赖未重启。
- 备份目录 `/opt/backups/silkroadai-portal/releases/sale-multiplier-20260916-063922/`，环境备份同后缀；回滚标签 `rollback-sale-multiplier-20260916-063922` 指向旧运行cb8aa91。79条migration、11表/客户档次权限摘要及new-api配置原始字节保持，既有1条撤销任务/15明细完整保留。50/50网络、13/13持久鉴权、两类UA模型列表和公网新UI资源hash通过；无真实改价或收费。
- 成本JSON新增可选`retail_multiplier`，旧镜像不能读取新字段。回退前必须暂停新写入并核对规则/历史、成本任务及未决状态；存在新格式数据时不得盲回旧版，也不能删字段变成零加价。详情、备份和验收限制见 [售价倍率发布报告](../docs/SALE-MULTIPLIER-2026-09-16.md)。后续纯文档提交不重建镜像。

## 定价计算与发布合并（2026-09-16，17:57 已上线）

- 运行源码 `prod@77a044d`，镜像 `sha256:78094a3e09c6fb6929cdd669c70c7951c27ed18b7c8ee4245ac64d981ad7ff86`。管理员只保留「定价」导航，旧计算器地址自动跳转；该页可查LiteLLM基础参考价、填充值比例与双倍率、查看人民币成本/售价，并保存当前规则后直接预览。动态/缓存/视频发布限制保持。
- 无migration/env/Nginx/Cloudflare变化。完整4705项测试、类型/Prisma/构建通过，lint0 error/93既有warnings；生产备份克隆23项主检查及真实认证页面、4组旧路由参数、保存/历史/签名预览通过，临时容器/库/含密文件已清理。
- 备份目录 `/opt/backups/silkroadai-portal/releases/unified-pricing-20260916-094624/`，环境备份同后缀；回滚标签 `rollback-unified-pricing-20260916-094624` 指向旧运行79c0928。两版成本JSON兼容，回退仍须核对未决任务并阻止新写入。
- 17:56:59–17:57:14切换Portal，首次失败至恢复采样8.925秒；restart0、日志error0，依赖未重启。79条migration、11张业务表/客户权限及new-api配置UTF-8原始字节保持；50/50网络、14/14持久鉴权、两类UA模型列表和公网新资源hash通过。无实际改价、收费生成或支付。
- 本地浏览器验收普通模型查询/填入/保存预览、编辑失效旧预览、保存成功后预览503与375px浅深色；动态模型试算可保存但仍禁止普通发布。详情见 [统一定价发布报告](../docs/UNIFIED-PRICING-2026-09-16.md)。后续纯文档归档不重建镜像。


## GPT-5.5 阶梯与缓存定价闭环（2026-09-17，10:06 已上线）

- 运行源码 `prod@bf312e6`，镜像 `sha256:3f33448b7ad48369405a0a33baf1637c689f6f50da6e3c6f7d64a01953307cf9`。定价页可从既有草稿预览完整阶梯、缓存读取、客户专属倍率，确认后由持久任务核对管理 API、只读 MySQL 和匿名运行价格，再保存完整目录；同目标不重复写价。公开页、机器目录、后台历史保留完整阶梯。
- 新迁移 `20260916160000_catalog_tiered_pricing` 仅新增可空 JSONB 列 `catalog_prices.billing_details`。生产副本 26 项检查、4,929 项完整测试、类型/构建/lint 0 error、桌面浅深色与手机浏览器验收通过后，正式 79→80 迁移，checksum 全匹配。未改生产价格或已有草稿。
- 备份目录 `/opt/backups/silkroadai-portal/releases/tiered-pricing-20260917-015341/`；环境备份同后缀；回滚标签 `rollback-tiered-pricing-20260917-015341` 指向旧运行77a044d。一旦存在完整阶梯元数据或V3任务，禁止盲回旧镜像，不能删除业务数据强行回退。
- 10:06:01–10:06:16 切换 Portal，失败样本跨度7.420秒后恢复；restart0、日志error0、依赖未重启。50/50网络、14/14持久鉴权、两类UA模型列表、公网UI资源hash与原始配置/11表摘要通过。
- 生产仅验证已有GPT5.5企业草稿preview：revision1保留，返回普通输入0.8/4.8/缓存0.08，完整输入≥272000为1.6/7.2/缓存0.16，专属倍率单列，unchanged=true。无保存/发布/PUT。用户刷新定价页即可继续预览确认；只有任务“已生效”表示核验完成。
- 本次文本线性阶梯及缓存读取通过；复杂公式、缓存写入时长、视频与未适配分辨率规则仍按明确能力拒绝发布。完整范围与证据见 [阶梯定价发布报告](../docs/PRICING-TIERED-2026-09-17.md)。后续纯文档归档不重建镜像。

## 统一倍率定价修正（2026-09-17，11:44 已上线）

- operator明确阶梯仅为了解上游策略，不是客户定价需求。运行源码 `4be57f9` 改为基础报价×售价倍率÷充值比例得到所有输入长度统一单价，上游倍率只用于参考成本；新任务V4，历史V3保持原意。镜像 `sha256:dcfd2cc626a1c1005bd3da964ec2e9aeb48177b8a635c7d9d905531cc4306ba3`，后续文档提交不重建。
- 无migration/env/Nginx/Cloudflare变化。4,983项测试、类型/构建/lint0error、官方引擎96向量、隔离实PG/new-api四个成功发布任务及桌面浅深色/手机版通过。生产备份副本26项通过，80条migration仅status核对，临时资源清理。
- 备份目录 `/opt/backups/silkroadai-portal/releases/uniform-pricing-20260917-033418/`；环境备份同后缀；回滚标签 `rollback-uniform-pricing-20260917-033418` 指向旧bf312e6。存在V4任务或未知/未决写入禁止回退旧worker，不得删除记录强行回滚。
- 11:44:16–11:44:31切换Portal，失败样本跨度7.623秒后恢复，restart0/启动error0，依赖不变。50/50网络、14/14持久授权与计费、两类UA模型列表、资源hash通过；新鲜基准的11表及new-api配置原始字节保持。
- 生产仅预览既有草稿，统一目标¥0.8/¥4.8/¥0.08（每百万token），专属配置¥0.9/¥5.4/¥0.09，旧草稿revision1保持，jobs/writes仍0。管理员刷新后核对预览并确认才改实际扣费；本次无真实改价或收费。详见 [统一定价修正报告](../docs/UNIFORM-PRICING-2026-09-17.md)。

## 定价预览去除旧阶梯对比（2026-09-17，14:52 已上线）

- 应用 `d53fd79`，镜像 `sha256:30abb512cf3e2b4ba7d4a5a71c807faa158178729bb7a904b6b04352ddea4033`。
  仅精简预览展示：移除普通/专属客户的旧长度分档对比，隐藏未配置的缓存写入空行，直接显示发布后售价。
  后端计费与发布校验保持，无 migration/env/依赖/Nginx/Cloudflare 变化。
- 完整 4983 项测试、类型/构建/格式、lint 0 error 和浅深色/手机检查通过；备份实际恢复核对后，仅切换
  Portal。14:51:53–14:52:08 切换，失败采样跨度 8.126 秒，随后稳定，restart0/启动error0，依赖未重启。
- 备份目录 `releases/pricing-simple-20260917-064404` 位于 `/opt/backups/silkroadai-portal/`；环境备份
  `.env.bak.pricing-simple-20260917-064404`；回滚标签 `rollback-pricing-simple-20260917-064404`
  属于 `silkroadai-portal-portal`。前后 11 表、80 条 migration、12 项价格配置摘要保持。
- 50/50 源站/公网及生产只读检查通过，公网 JS 与镜像一致，旧对比文案消失。jobs/writes 仍 0，未替用户
  发布价格。后续文档提交不重建镜像；详细证据见 [发布补充](../docs/UNIFORM-PRICING-2026-09-17.md)。

## 缓存写入统一定价（2026-09-17，21:32 已上线）

- 应用 `6138d5b`，镜像 `sha256:2f3426bfc78e6657588e6e6a71ddee16127c2c24b5103ef8a476c2f62c0c6f41`。GPT-5.6 Sol 等模型补齐缓存写入保存、预览、确认、持久任务与回读核验，客户仍按统一单价计费。新任务 V5，历史 V3/V4 语义保持；可选 `cache_write_1h` JSON 无默认值。
- 完整 5039 项测试、类型/构建/lint 0 error、官方引擎 168 向量、隔离真实 HTTP/PG/MySQL 14 组检查与浏览器预览通过。隔离 new-api HTTP 使用合成边界，未收费调用；生产克隆 14 项主检查通过。无 migration/env/依赖/Nginx/Cloudflare 变化。
- 备份及证据目录 `/opt/backups/silkroadai-portal/releases/cache-write-20260917-084958/`；环境备份 `.env.bak.cache-write-20260917-084958`；回滚标签 `rollback-cache-write-20260917-084958` 属于 `silkroadai-portal-portal`，旧应用 `d53fd79`。有 V5 任务、新 1 小时成本数据或未决写入时，禁止盲回旧镜像或恢复数据库。
- 21:32:36–21:32:52 切换 Portal，首次失败至恢复探测跨度 8.925 秒，随后稳定；restart0、启动error0，依赖未重启。50/50 网络检查、80 条 migration、11 表及 15 项价格配置摘要保持，公网脚本与新镜像一致，线上 Sol 能力已为可发布且要求缓存读取/写入报价。
- 既有 GPT-5.5 价格、客户 Key、余额保持；没有替用户发布生产价格。后续纯文档归档不重建镜像；完整范围与验收边界见 [缓存写入定价报告](../docs/CACHE-WRITE-PRICING-2026-09-17.md)。

## 共享模型分组定价修正（2026-09-23，09:12 已上线）

- 运行源码 `prod@797b77d`，镜像 `sha256:6a0c9529797924b8cfb4ac9d7e5b18be1aa1e260b51472d17787f5d2da5f6eb9`。`3bc21fd` 修正分组价格精度与预览；`797b77d` 允许同一模型属于多个分组，修改所选组时只写该组 `GroupRatio`，保留共享基础价并预览该组实际价格。没有发布生产价格。
- 无 migration/env/依赖/Nginx/Cloudflare 变化。`pnpm test:ci` 为 342 files / 5336 passed / 1 skipped；typecheck、生产构建通过；lint 0 errors / 96 warnings。全仓格式检查仍被 6 个既有文件阻断，本次 3 个源码文件的 Prettier 检查通过。
- `.env.bak.20260923-090309` 与 `/opt/backups/silkroadai-portal/portal-20260923-010309.sql.gz` 均为 0600，数据库备份 gzip 完整；80 条 migration 均已应用，无 pending/rolled back。Portal restart count 0，PostgreSQL/new-api 未重启；Portal→new-api 200，主站登录页 200、API 假 Key 401、Nginx 配置有效且 active。
- 回滚标签 `silkroadai-portal-portal:rollback-20260923-090309` 指向 `sha256:e0cc57cdb22cba3ffb9a9385dd156d564555753509f8b9416e15f2a1e91b5499`，由干净 Git 归档 `00b22c9` 重建。9 月 20 日原运行镜像 `sha256:b6942a52474d41aa5d9dc7984579b4873e5a30a66cf01fb78babfff1f210fe8e` 当时已不在 VPS 镜像库；本标签是源码版本对应的重建镜像，不是原镜像的字节级副本。重建未影响运行中的 `release-797b77d`。
- 完整发布和回滚核验证据见 [共享模型分组定价发布记录](../docs/DEPLOY-GROUP-PRICING-2026-09-23.md)。后续纯文档提交不重建镜像。

## 官方全局基础价与档次倍率分离（2026-09-23，17:34 已上线）

- 运行源码 `prod@d8509a2`；官方模型基础价固定按 `GroupRatio=1` 定义，客户档次价格由各自未修改的 `GroupRatio` 派生。新增全局模型定价工作台/API、档次倍率独立发布与恢复校验，以及全局任务的持久计划重建校验。
- 无 Prisma migration、环境变量、依赖、Compose、Nginx 或 Cloudflare 变化；生产保持 80 条 migration，未执行真实改价或收费调用。
- `.env.bak.20260923-172812` 与 `/opt/backups/silkroadai-portal/portal-20260923-092812.sql.gz` 均为 0600，数据库备份通过 `gzip -t`。构建上下文排除了全部 `.env*`。
- 新镜像 `silkroadai-portal-portal:release-d8509a2`，digest `sha256:4050de372cd352011badb647b8560de99b3e90da96a075456c0ebc57f2ec40ab`；回滚标签 `rollback-global-pricing-20260923-172812` 指向旧运行镜像 `sha256:6a0c9529797924b8cfb4ac9d7e5b18be1aa1e260b51472d17787f5d2da5f6eb9`。
- Portal restart count 0、PostgreSQL healthy、Portal→new-api 200、Nginx 配置通过；主站 200、API 假 Key 401、API 非白名单路径 404、两类 CORS 204、OAuth start 302。发布协调器无活动任务、无未终态任务、无 `in_flight` 写入。
- 详细证据见 [官方全局基础价发布记录](../docs/DEPLOY-PRICING-GLOBAL-BASE-20260923.md)。后续纯文档提交不重建镜像。

## 两旋钮简化定价（2026-09-24，16:25 已上线）

- 运行源码 `prod@3bdc5da`（功能提交 `8c847dd`，`3bdc5da` 仅为文档格式化）。`/admin/pricing` 改为「每个上游模型一个基础价 × 每个档次一个 `GroupRatio`」：保存同步事务执行，带乐观锁、回读核验、`CatalogPrice` 快照与 `AdminAuditLog`；阶梯价保留；`GroupRatio` 两个键自动同写。新增 `GET/PUT /api/admin/pricing/simple`。没有替用户发布或修改生产价格，也没有收费调用。
- 无 Prisma migration、环境变量、依赖、Compose、Nginx 或 Cloudflare 变化；生产保持 80 条 migration，启动日志 `No pending migrations to apply`。
- 本地发布门：`pnpm test:ci` 346 files / 5392 passed / 1 skipped；typecheck 通过；lint 0 errors / 96 warnings。全仓格式检查仍被 5 个既有文件及 1 个未提交的用户文档阻断，本次提交涉及的文件均已通过 Prettier。
- `.env.bak.20260924-161137` 与 `/opt/backups/silkroadai-portal/portal-20260924-081138.sql.gz` 均为 0600，数据库备份通过 `gzip -t`。构建上下文为 Git 归档并排除全部 `.env*`。
- 首次构建因拉取 Google Fonts 网络瞬断失败（`@vercel/turbopack-next/internal/font/google/font`），线上未受影响；原命令重跑成功。新镜像 `silkroadai-portal-portal:release-3bdc5da`，ID `db5dbacb2d8a`。
- 16:25:37 只重启 `portal`，约 9 秒后 `/login` 恢复 200；restart count 0，PostgreSQL/new-api 未重启。Portal→new-api 200；源站、apex、`www` 登录页均 200；`/api/admin/pricing/simple` 未登录 401，`/admin/pricing` 307，`/v1/models` 无 Key 401；`nginx -t` 通过且 active。
- 启动瞬间有一次 `[pricing-publisher] attempt interrupted; durable intent retained`，之后未再出现。只读核对：`active_job_id` 为空，jobs 仅 `succeeded=3`，writes 仅 `acknowledged=3`，无未决任务或 `in_flight` 写入。
- **本次没有回滚镜像。** 回滚标签在 build 之后才打，此时旧运行镜像（`f3e9a81b5cb5`，源码 `402f4e4`）已因 `latest` 指向新镜像而在 containerd 存储中不可寻址，`docker tag` 与 `docker commit` 均失败。自本次起 4.4 已改为 build 前打标签并核对。需要退回 `402f4e4` 时只能按 8.2 重新构建（无 migration 差异，约 5 分钟）；紧急时可临时改用 `release-d8509a2`（仅缺「查询官方基础价」）。下一次发布的回滚点为 `release-3bdc5da`。
- 本次通过临时部署密钥 `claude-temp-deploy-20260924` 登录，发布后已从 `authorized_keys` 删除并验证无法再登录。后续纯文档提交不重建镜像。
